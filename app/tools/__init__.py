"""Tool registry package — single source of truth for agent/MCP-exposed capabilities.

See `app.tools.registry` for the `ToolSpec`/`@tool` mechanics, `app.tools.context`
for `ToolContext`, `app.tools.invoke` for runtime dispatch, and `app.tools.catalog`
for the concrete tool wrappers over existing services.
"""
