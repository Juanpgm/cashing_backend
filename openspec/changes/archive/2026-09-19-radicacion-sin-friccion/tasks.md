# Tasks: Radicación Sin Fricción

Mirrors the phase/slice numbering in the published plan (artifact `e9c67d19-e335-4cab-9cc9-d7f3e5a7b513`) and Engram topic `plan/radicacion-sin-friccion`. Each slice: implement with strict TDD → name and cover its edge cases → self-verify → fresh-context adversarial review (opus) → PR. State kept in sync in this file, in Engram (`sdd/radicacion-sin-friccion/state`), and via `codebase-memory-mcp` re-index after each backend/frontend merge.

Legend: `[ ]` not started · `[~]` in progress / delegated, awaiting result · `[x]` done and reviewed · `[!]` blocked

## Checkpoint 2026-09-19 (amended after the first `sdd-verify` run)

- **Masters at this checkpoint**: backend `cashing-backend-master` `81aab19` (PR #95) · frontend `cashing-frontend` `11bf4c4` (PR #71). The original as-built checkpoint was backend `a730acf` (PR #92) / frontend `db45c9f` (PR #67); everything merged after it is recorded in **Phase 5** (5.1–5.8), which is not part of the published plan — it exists so this file matches `master`.
- **Task count**: 55 numbered slices, all `[x]` (46 original + 4.8 closed out + 8 in Phase 5). No `[~]`, `[!]` or `[ ]` remain.
- **Suites at the checkpoint**: backend `uv run python -m pytest` **3329 passed, 1 skipped** at `81aab19` (the skip is the Postgres-only `test_radicar_lock_bloquea_segunda_transaccion_bajo_postgres`; the last PR's gate line counts it as `tests=3330`); frontend vitest **1389 passed** at `11bf4c4` (`GATE OK b19179f unit=1389 e2e=7 …` on PR #71, a test-only change).
- **Live stack runs recorded during the remediation** (backend master `81aab19`, `LLM_PROVIDER=fake`, SQLite): journey **13/13**; edge-cases **18 passed / 1 skipped**; `radicacion-paquete-completo` passes. PR #70's body reports the combined paquete-completo + edge-cases run as 26 passed / 1 skipped.
- **Backend lint/type backlog** (non-blocking by design, part of the gate line): `ruff=526 mypy=357` at `81aab19` versus the `ruff=501 mypy=336` recorded in 4.3; per-PR gate lines are in 5.1, 5.2 and 5.5. **Frontend lint backlog** went `5e/8w` → `3e/8w` with PR #69 (2 `lib/checklist-api.ts` errors removed).
- **Open user decision** (recorded, not decided): group-2 height — see `design.md` Open Questions and 5.3.

## Continuation 2026-09-13 — re-sequenced execution order (plan file `~/.claude/plans/federated-cooking-corbato.md`)

Re-verified every pending slice against clean `master` in both repos (backend `de9062d`, frontend `34c1cd7`). Decisions locked: production E2E is **read-only** (Engram `plan/radicacion-sin-friccion/prod-e2e-policy`); GitHub Actions is **billing-locked in both repos** (runs die in 0-4 s with `startup_failure`) so every gate is local until the user fixes billing. New slices: **0.6** (fake-LLM ID threading, ex 0.4 F-2), **1.5a/1.5b** (split), **1.10** (local pre-merge gate), **3.2a** (consumer audit), **4.9** (production smoke).

Order: 1.2 → 1.3 → 1.4 → 4.9 → 1.10 → 4.5 → 4.4 → 0.6 → 1.5a → 1.5b → 4.1 → 4.6 → 4.7a → 2.1 → 2.2+2.3 → 2.5 → 2.4a/b → 4.2 → 3.1 → 3.2a → 3.2 → 3.6 → 4.7b → 3.8 → 3.7 → 3.9 → 2.6 → 2.7 → 3.3 → 3.4 → 3.5 → 1.6 → 1.7 → 1.8 → 1.9 → 3.10 → 4.3 → 4.8.

Drift found vs. the original entries (already folded into the slices below): 1.4's code map has a phantom `GOOGLE_NOT_CONNECTED` and misses 4 real backend codes; 1.5 is **18** hand-rolled `err.detail` sites, not 3; 1.9's existing test drives tools directly (no `ScriptedLLM`); 3.6 has **4** `window.confirm` sites, not 3; `playwright.config.ts` hardcodes `baseURL` and always boots `webServer`.

## Phase 0 — Foundations & measurement (repo: workspace root + `cashing-backend-master`)

- [x] **0.1** Repoint `start-local.ps1` / `start-local-neon.ps1` to `cashing-backend-master`.
  Files: workspace-root `start-local.ps1`, `start-local-neon.ps1` (not under git — no PR, filesystem-only). `kill-local.ps1` needed no change (port-only, no path reference).
  Verified: target path resolves (`scripts/start-local.ps1` exists under `cashing-backend-master`).
- [x] **0.2** Query-count regression fixture (`before_cursor_execute` counter) + per-endpoint budgets in `tests/conftest.py`.
  **Measured (real, re-measured after review, not estimated)**: contratos list **11**, contrato detail **19** (both match audit), checklist **47** (audit estimated ~36-40 — measured higher: a real uploaded document, not just cumplido_manual rows, is production's actual shape), radicar **80** (audit estimated 60-80, at the edge of range), stepper-state **24** (matches audit ~25). `radicar` inherits checklist's cost since it re-runs `construir_checklist_completo` before flipping estado.
  Edge cases covered: nested SAVEPOINT counting, reset-between-calls, budget-failure message clarity, listener-leak cleanup (verified 0 listeners after teardown), Postgres skip guard (module skips, doesn't falsely fail, when `TEST_DATABASE_URL` is Postgres).
  Files: `tests/conftest.py` (`QueryCounter` class + `query_counter` fixture, reusable by Phase 2), `tests/test_query_counter_fixture.py` (new, 5 tests), `tests/test_query_budgets.py` (new, 5 tests). Full suite: 2401 passed, 4 skipped, 0 failed. Ruff clean.
  Branch `chore/phase0-query-budget-fixture` @ `3450fb9`. **Fresh-context adversarial review (reliability axis): GO**, with 2 findings folded in before PR (degenerate-checklist budget re-measured with a real upload 44→47/77→80; Postgres skip guard added) and 1 tracked as follow-up. PR: [#66](https://github.com/Juanpgm/cashing_backend/pull/66) — **MERGED** to master.
  **Follow-up tracked (not blocking)**: `QueryCounter.assert_budget` is upper-bound only — Phase 2's query-count reductions won't auto-tighten the ratchet without an exact-match/floor mode.
- [x] **0.3** Instrument the agent turn: `duration_ms` on `ToolEvent`, `perf_counter` around `llm.complete`/`invoke_tool`, structlog `contextvars`-bound `session_id`, iteration count, model + fallback depth, prompt/completion token split. Wire the existing (0-call-site) Langfuse tracer around `_call_model`. Gate NOTSET log level by environment.
  Edge cases: instrumentation must not throw and abort the turn if Langfuse is unreachable; timing fields present even on a tool-error branch; two concurrent sessions' contextvars must not leak into each other's log lines.
  Files (final): `app/core/observability.py` (new, `elapsed_ms` helper), `app/schemas/agent.py` (`ToolEvent.duration_ms`, `LLMResponse.fallback_depth`), `app/adapters/llm/litellm_adapter.py` (timing + fallback_depth + Langfuse `_trace_generation` wired, fail-open), `app/services/agent_chat_service.py` (`chat_with_tools` split into bind/unbind wrapper + `_run_chat_turn`, per-tool `duration_ms`, final summary now includes `iterations_run`/`turn_duration_ms`), `app/main.py` (`_wrapper_log_level()`, DEBUG only in dev). New tests: `tests/test_agent_chat_observability.py` (6), `tests/test_litellm_adapter.py` (+5), `tests/test_main_log_level.py` (3).
  Full suite: 2405 passed, 4 skipped, 0 failed (run twice, same result). Concurrency edge case verified with two real concurrent `chat_with_tools` turns via `asyncio.gather` — surfaced a real structlog gotcha, see [[gotcha-structlog-cache-logger-processors]].
  Branch `chore/phase0-agent-observability` @ `d54fc53`. **Fresh-context adversarial review (resilience axis): CONDITIONAL GO**, 3 findings fixed before PR (Langfuse wiring was a permanent no-op in every environment since `langfuse` isn't a declared dependency — made the disabled/misconfigured state loudly visible via structlog instead of adding the dependency unilaterally, a real cost/product decision left to the user; 2 operational log lines the new log-level gate had silently demoted to invisible-in-production — `pago_service.webhook_ignorado` and the agent's contract-context-not-resolved branch — promoted debug→info/warning; `ruff format` actually applied, the initial "clean" claim was false for 2 files). PR: [#67](https://github.com/Juanpgm/cashing_backend/pull/67) — **MERGED** to master.
  **Follow-ups tracked (not blocking, real gaps for a future slice)**: no error tracker or CI exists to consume the new metrics; failure-path LLM calls still have no `duration_ms` (only success paths are timed); no `retry_count` distinguishing a slow model from a flaky one; `unbind_contextvars` doesn't restore an outer binding (harmless today — only one bind site in the app).
- [x] **0.4** Deterministic LLM seam for the *running app* (not just tests): `LLM_PROVIDER=fake` (new `Literal["litellm","fake"]` setting, invalid values fold to `"litellm"`) → `FakeLLMPort`, stateless, routes by last-tool-called name. `synthesize_tool_arguments()` produces schema-valid calls for all **32/32** catalog tools (parametrized test), not just the happy-path subset. End-to-end verified: `chat_with_tools()` completes the 6-tool happy path (listar_contratos → crear_cuenta_cobro → definir_requisitos_checklist → importar_documento → resumen_checklist → radicar_cuenta) with `LLM_PROVIDER=fake` and no test-only `ScriptedLLM` opt-out.
  Bonus fix: a pre-existing bug in `app/adapters/llm/port.py`'s `stream()` Protocol stub (unimported forward ref + coroutine/async-generator mismatch), only surfaced because `get_llm()`'s return type widened to the `LLMPort` protocol.
  **Self-disclosed limitation, needs review verdict**: routing advances by tool name only, not by real IDs from prior tool results — `crear_cuenta_cobro` gets a synthesized random `contrato_id` that would 404 in a real run. May or may not be sufficient for Phase 4's Playwright use case — flagged explicitly to the reviewer.
  SECOP fixture (item 3) not attempted — deprioritized in favor of hardening the LLM fake.
  Second occurrence of the structlog `cache_logger_on_first_use` gotcha found (order-dependent, `tests/test_secop_configuracion.py`) — workaround applied, root fix tracked as follow-up, see [[gotcha-structlog-cache-logger-processors]].
  Files: `app/adapters/llm/fake_adapter.py` (new), `app/core/config.py` (`LLM_PROVIDER`), `app/adapters/llm/litellm_adapter.py` (`get_llm()` dispatch), `app/adapters/llm/port.py` (bugfix), `tests/test_fake_llm_adapter.py` (new, 55 tests). Full suite: 2442 passed, 4 skipped, 0 failed.
  Branch `chore/phase0-fake-llm-adapter` @ `3afada2`. **Fresh-context adversarial review (reliability axis): NO-GO** — the first real no-go in this plan's review history. Confirmed findings: (a) BLOCKER — the E2E test asserted only tool NAMES, never `status`; 5 of the 6 happy-path tool calls actually fail with "not found" domain errors (synthesized UUIDs don't exist) and the test was green anyway; (b) CRITICAL — `build_tool_call`'s `model_validate` is unguarded, a `ValidationError` there escapes as a misdiagnosed "LLM network failure"; (c) WARNING — `LLM_PROVIDER` matching is exact-case, `"FAKE"`/`"Fake "` silently fall through to the REAL provider (real cost/determinism risk in CI); (d) report-accuracy — test count (51, not 55), full-suite baseline (2391, not 2405), and a false claim of fixing an unrelated `test_secop_configuracion.py` flake were all wrong in the original commit message.
  **Reviewer's explicit verdict on the question asked**: the synthesized-ID limitation DOES block Phase 4 — a real radicación chain 404s at step 2 (`crear_cuenta_cobro`), and there is no cross-turn continuity (a 2nd chat turn on the same session replays the identical 6-call chain from scratch instead of progressing). **Phase 4 cannot rely on this seam as "a deterministic happy path" — that needs its own future slice (outcome-aware routing that threads real IDs from prior tool results + cross-turn state), tracked here, not attempted in 0.4.**
  Confirmed fixes (F-1/F-4/F-5/F-6/F-7/F-8) applied on the SAME branch (not a fresh fork), commit `f8a2b62` — explicitly scoped OUT: F-2 (cross-turn/ID-threading) and full type-coverage for F-3's untested synthesizer branches, both left as tracked follow-ups.
  **That fix-pass itself introduced a real full-suite regression**, caught by re-running the suite (not trusting the sub-agent's "not a regression, pre-existing" claim): `2449 passed, 1 failed` — F-5's new `structlog.warning()` call shares `app.core.config`'s module-level `_log` proxy, shifting when `cache_logger_on_first_use=True` first caches that logger across the 2450-test suite, tripping `tests/test_secop_configuracion.py`'s `capture_logs()`-based tests. **Third occurrence of the same structlog order-dependency class** already tracked twice in this plan (0.3's concurrency test, 0.4's own original SECOP_APP_TOKEN workaround) — see [[gotcha-structlog-cache-logger-processors]].
  Fixed for real this time, commit `20c5d91`: rewrote the two affected tests to monkeypatch `app.core.config._log.warning` directly instead of depending on global `structlog.testing.capture_logs()` state — order-independent by construction, not another workaround. Verified: full suite **2450 passed, 4 skipped, 0 failed**, isolated and full-suite runs both green, ruff clean.
  **Third review round (commit `20c5d91`): GO on substance, blocked on a race.** Independently verified F-1/F-4/F-5 all genuinely hold via execution AND mutation testing (deliberately broke each fix, confirmed the test then correctly fails). Confirmed the full suite was truly green at that exact commit. Also found the monkeypatch fix's own real defect: `monkeypatch.setattr()` on a structlog lazy-logger-proxy doesn't undo cleanly (`getattr` synthesizes a value via `__getattr__`; teardown then installs a permanently-frozen method instead of removing the patch) — provable via mutation, though not yet biting in that specific run.
  **Root-caused for real, commit `a07f6d8`.** Traced the leak to its actual source: a *different* test (`test_llm_provider_invalid_value_logs_a_warning`, added by the earlier fix-pass) used the identical leaking monkeypatch pattern and runs alphabetically before the test it was poisoning. **Empirically disproved my own first hypothesis along the way**: disabling `cache_logger_on_first_use` under pytest alone did NOT fix the full-suite failure (tested it, still 1 failed) — the actual cause was the monkeypatch leak, not caching. Removed the leaking pattern from both call sites (both now use `structlog.testing.capture_logs()`, already proven robust by round 3); kept the `cache_logger_on_first_use` pytest-gate anyway as independently-safe defense-in-depth, explicitly not claimed as *the* fix. Verified: full suite **2450 passed, 4 skipped, 0 failed**, run twice, default order.
  PR: [#68](https://github.com/Juanpgm/cashing_backend/pull/68) — **MERGED** to master (required a manual conflict resolution against #67, both touched `app/main.py`/`litellm_adapter.py` — resolved by combining both changes, full suite re-verified green: 2478 passed post-merge). Full honest round-by-round history in the description, including the corrected baseline (**2391 passed + 4 skipped** — wrong twice before, verified independently by round 3's reviewer via `pytest --collect-only`).
  **Still open, tracked, not fixed in this slice**: F-2 (cross-turn ID-threading, blocks Phase 4 relying on this as a working happy path) and F-3's 6 informally-tested synthesizer type branches.
- [x] **0.5** Minimal CI: `.github/workflows/ci.yml` in each repo (backend: `uv run python -m pytest --cov=app` ≥70% + non-blocking ruff/mypy + nightly real-Postgres job; frontend: `npm run test:unit` + `tsc --noEmit` + `npm run build`).
  Verified locally: both YAML files parse; frontend `npm run build` succeeds without Firebase env vars (15/15 static pages) — confirms no CI secrets needed for that job.
  PRs: backend [#65](https://github.com/Juanpgm/cashing_backend/pull/65), frontend [#41](https://github.com/Juanpgm/cashing_frontend/pull/41) — **both MERGED**. Note: backend PR #65's own first CI run FAILED both jobs — not a code problem, GitHub Actions reported "your account is locked due to a billing issue" (the jobs never started). This blocks live CI validation on EVERY future PR in this repo until resolved on GitHub's billing page — flagged to the user, not something fixable from here.

- [x] **0.6** (NEW 2026-09-13, ex 0.4 F-2) `FakeLLMPort` outcome-aware routing + real-ID threading, derived purely from `messages` (no instance state): `_tool_results()` pairs each assistant `tool_calls[0]` with its `role="tool"` message; `_known_ids()` collects UUID-shaped `id`/`*_id` from parsed results aliased per tool (`listar_contratos.id → contrato_id`, `crear_cuenta_cobro.id → cuenta_id`, same convention as `_extract_recap_ids` in `agent_chat_service.py:825`); `synthesize_tool_arguments(model, known=None)` prefers known IDs; script position = last tool whose result has no `"error"` (retry once, then text); cross-turn continuity via the `AGENT_RECAP_MARKER` system message; honor `response_format` by returning JSON (12 `json.loads` call sites); `FAKE_LLM_SCRIPT=happy|malformed|stall` env.
  Edge cases: result missing `id`; two tools returning the same key; malformed tool content; recap present but truncated; concurrent sessions don't share IDs; the 6-tool happy path completes with every `status` ok against a real local DB.
  Files: `app/adapters/llm/fake_adapter.py`, `app/core/config.py`, `tests/test_fake_llm_adapter.py`. Hard prerequisite for 4.7a/4.8.
  **Done 2026-09-13.** Branch `feat/phase0-fake-llm-id-threading`, 8 commits. Real discovery: `listar_contratos`'s tool result is pipe-delimited compact text (`agent_chat_service._compact_listar_contratos`), not JSON — a tool-specific parser was added, verified end-to-end via a direct DB assertion, not just tool-name matching. Review (real-shape-correctness axis): CONDITIONAL GO — CRITICAL: the `resumen_checklist` alias was documented "load-bearing" but dead on the live serialized path (its compact text carries no id), the existing test fed it a payload the app never produces; fixed by correcting the docstring (recap-only, proven correct there) and rewriting the test against the real compact shape. WARNING: unscripted-tool retry bypassed the membership check; silent unparseable-content fallback with no log — both fixed. Suite 98 in the adapter file, full suite 2542 (+39). PR [#72](https://github.com/Juanpgm/cashing_backend/pull/72) — **MERGED**.
  Follow-ups tracked: nested-`BaseModel` `response_format` fields still degrade to `"{}"` (confirmed unreachable today, relevant to future document-ingestion Playwright runs); happy-path steps 5-6 have no end-to-end DB-level guard yet (E2E stops at step 4, `importar_documento`, which correctly fails without a real attachment).

**Phase 0 exit criteria**: CI green in both repos; query-count and timing baselines recorded in Engram; a chat turn completable with `LLM_PROVIDER=fake` and no network.

## Phase 1 — Close the loop (repos: both)

- [x] **1.1** Step 7 "Finalizar" calls `radicar`; renders real post-radicar state (estado, fecha, ZIP link). Remove the step-3-group radicar button once step 7 owns it.
  Edge cases: incomplete checklist shows named pendientes; `COHERENCE_CHECK_FAILED` surfaces the finding; network failure keeps the button retryable, doesn't fake success; double-click/already-enviada; unrecognized error code fallback; ResumenCard no longer offers to radicate.
  Files: `components/stepper/steps/step-7-paquete.tsx`, `components/checklist-full-view.tsx`.
  Implemented: `step-7-paquete.tsx` owns the radicar mutation (reuses `radicarCuenta`/`obtenerCuentaCobro` from `lib/cuentas-cobro-api.ts` verbatim, no new API call), footer button renamed "Radicar cuenta", hardcoded "¡Radicación lista!" closing screen replaced with real post-radicar data. `checklist-full-view.tsx`'s `ResumenCard` radicar button removed, readiness summary kept with guidance text pointing to step 7. `codeMessage()`'s shared raw-code fallback left untouched for other call sites; a local wrapper in step 7 substitutes a generic message specifically for radicar so no raw code reaches the user there.
  Tests: 12 new (3 `checklist-full-view`, 9 full-mount `stepper-radicar.test.tsx` covering every required edge case). Full suite 570/570 passed. `tsc --noEmit` clean.
  **Self-disclosed regression, needs review verdict before push**: `app/cuentas-cobro/[id]/radicacion/page.tsx` (the legacy tabs page, which also renders `ChecklistFullView` but has no step 7 of its own) now has **no reachable way to radicate a cuenta** — this slice's own scope removed its only path without the Phase 3.2 redirect (which retires this page entirely) having landed yet. Real interim gap for anyone still using the legacy page.
  Branch `feat/phase1-wizard-radica` @ `6075de6`. **Fresh-context adversarial review: NO-GO.** Confirmed the core move is right (real API reuse, correct invalidation keys, real success data) but found 3 blockers + 1 near-blocker, each reproduced against the real components (not just read):
  1. **Legacy-page regression IS reachable from a primary flow, confirmed** — creating a cuenta from `/contratos/{id}` auto-navigates to `/cuentas-cobro/{id}/radicacion` (`app/contratos/[id]/page.tsx:144`), which after this branch has no way to radicate. Not orphaned as hoped; needs a companion fix (pull forward part of Phase 3.2: redirect that page to `/radicar/{id}`, retarget the `router.push`).
  2. **Raw English error text still leaks** — the banner is sanitized but a separate "finding" line falls through to `err.message` for codeless errors (`"Network Error"`, `"Internal Server Error"` reproduced directly). Same failure class as 0.4: the test asserted the banner, never the finding line.
  3. **Double-click guard doesn't work** — reads `isPending` from a stale render closure; 3 rapid clicks fired 3 real POSTs in a probe. The branch's own test pre-waits for `disabled` before the 2nd click, making the assertion unable to ever catch this.
  4. **Near-blocker**: "Radicar cuenta" is enabled while the cuenta's own estado query is still loading (`undefined` reads as "not yet radicada") — a spurious radicar can fire against an already-`enviada` cuenta during that window. The removed `ResumenCard` had this exact guard; it wasn't ported.
  Plus 4 WARNINGs (non-durable success state that can revert on refetch race, a deleted readiness-gate test never replaced, no coverage for the cuenta-estado query itself failing, no CI enforcing any of this) and 4 SUGGESTIONs, all tracked, not blocking.
  **Second fix pass (commit `403ea57`), reviewed: NO-GO, narrowly.** Blockers 2 and 3 confirmed genuinely fixed via mutation-testing (reverted the fix, watched the new tests correctly fail against the old code). Near-blocker 4 only half-fixed — covers `cuentaQuery.isLoading` but not `.isError`, same spurious-radicar window survives when the estado query fails (reproduced: a click fires a real `radicarCuenta` call). Blocker 1's redirect approach itself was the wrong shape: it silently broke an EXISTING Playwright spec (`e2e/wizard-generar.spec.ts` clicks a "Generar" tab button that no longer exists on that route) and fully orphaned `GenerarTab`'s flow (Analizar → Semáforo → Generar, individual informe/evidencia downloads) — no other route renders it after the redirect, and the wizard's step 7 does not replace that functionality. Also re-pointed 3 other live entry points (including the read-only "Radicación" link shown for `aprobada`/`pagada` cuentas) into an editing wizard whose "Cerrar" navigates away, untested.
  **Corrected approach for the third pass**: do NOT redirect/retire the legacy page (that's Phase 3.2's job, with its own consumer audit) — instead add a minimal radicar action back onto the legacy page itself (reusing the same mutation logic), closing the actual gap without the redirect's blast radius. Also: widen the loading guard to `isLoading || isError`; stop suppressing legitimate codeless backend-composed Spanish messages (gate on "has a real `detail` from a response", not on "code is recognized"); fix the test helper that silently swallows clicks during the verifying-estado window.
  **Fourth review round: GO**, with one more real (non-blocking) finding fixed directly by the orchestrator: the loading/error guard's branches were ordered so a transient refetch failure right after a genuine radicar success could briefly mask the success behind "no pudimos verificar el estado" — reordered (`yaRadicada` checked before `cuentaQueryIsError`) in both consumers. Honest limitation: could not build a discriminating regression test for this exact race in this test harness (TanStack Query v5 keeps cached success data protected from a background refetch's `isError` in ways that made the scenario unreproducible here) — the fix is applied by inspection and the reviewer's explicit recommendation, documented plainly as untested in the commit, not claimed otherwise.
  PR: [#43](https://github.com/Juanpgm/cashing_frontend/pull/43) — **MERGED** to master, full 4-round review history in the description. Full suite 584/584, `tsc --noEmit` clean.
  **Follow-ups tracked, not fixed** (all confirmed non-blocking by round 4): stale footer hint during a transient error state; deleted client-side readiness-gate test has no positive replacement asserting "backend is sole source of truth"; a known error code's secondary finding line still shows a raw `detail` verbatim (low-likelihood residual risk); non-string `detail` can render as `[object Object]` (pre-existing in `extractApiError`, newly reachable here).
- [x] **1.2** `radicar` idempotent + lock-safe (`SELECT ... FOR UPDATE` or CAS `UPDATE ... WHERE estado='borrador'`); second call on an already-`ENVIADA` cuenta returns the existing result, not 422.
  Edge cases: two concurrent `radicar` calls (`asyncio.gather`) — exactly one transitions, neither duplicates side effects; radicar-after-rechazada; radicar-after-pagada (terminal) still rejects.
  Files: `app/services/cuenta_cobro_service.py:1021-1088`.
  **Done 2026-09-13.** Branch `feat/phase1-radicar-idempotente`, 4 commits. Design as landed: unlocked ownership read + ENVIADA short-circuit → gates unlocked → `_leer_estado_bajo_lock` (single-column `FOR UPDATE`, `nowait=` knob for tests) → CAS `UPDATE … WHERE estado IN (borrador, rechazada) AND deleted_at IS NULL`, `rowcount == 0` → return existing. Three review rounds: R1 CONDITIONAL GO (lock uncovered, held across ~76 queries, CAS ignored `deleted_at`); R2 GO (predicted the two Postgres failures before the run); R3 GO on `populate_existing`. **Real bug found only under Postgres**: `_reload_cuenta_response` returned the stale identity-map instance after another session committed → fixed with `populate_existing=True` (Engram `bugfix/reload-identity-map-stale-cross-session`). First real `make test-pg` of the plan, via scratch compose overrides (Engram `gotcha/test-pg-local-overrides`): 113 passed on pgvector/pg16. SQLite 2493 passed. Budget radicar 80 → 81. PR [#69](https://github.com/Juanpgm/cashing_backend/pull/69) — **MERGED**.
  Follow-ups: `_reload_cuenta_response` docstring must name the `autoflush=True` precondition (→ 1.3); `scripts/test-postgres.sh` assumes a free host port 5432 and a `.env` file (→ 1.10 backend half).
- [x] **1.3** `requisitos_modo` NULL resolved to one behavior everywhere (`crear_cuenta_cobro` materializes `estandar`, matching `asegurar_checklist`'s existing default).
  Edge cases: cuenta created without defining requisitos — `resumen_checklist`, `radicar`, and both informe tools agree; backfill migration for existing NULL rows.
  Files: `app/services/cuenta_cobro_service.py`, `app/services/checklist_service.py`, `app/tools/catalog/informes.py`.
  **Done 2026-09-13, design overturned by audit before coding.** NULL is a real tri-state the frontend depends on (`DefinirChecklistGate` on `requisitos_definidos`, `stepper_state_service` step-3 completion) — materializing at creation or backfilling would break the gate. **No migration written.** Real bug found: `POST /checklist/refresh-secop` and `.../auto-vincular-documentos` materialized a standard checklist unconditionally on a gate-unresolved cuenta while `GET /checklist` still said `requisitos_definidos=False`; gated both, added `checklist_service.modo_efectivo()`. Review (reachability axis): CONDITIONAL GO — the API bug was actually unreachable from the wizard (a frontend guard already blocked it), but the SAME bug was live on the agent-tool surface (`app/tools/catalog/checklist.py`'s `detectar_desde_secop`/`auto_vincular_documentos`, contradicting their own tool docstring) — fixed and mutation-verified. `radicacion_prep_service`/`cuenta_cobro_service`'s ungated callers investigated and left as-is (proven byte-identical row set + session-rollback-on-exception makes the risk moot). Suite 2503 passed (+4), no migration → no Postgres run needed. PR [#70](https://github.com/Juanpgm/cashing_backend/pull/70) — **MERGED**.
- [x] **1.4** Zero raw backend codes in the UI; fix `/cuentas-cobro/[id]`'s field mismatch (`valor_periodo`/`mes`/`anio` vs actual API shape) or redirect it to `/radicar/{id}`.
  Edge cases: every code `EXCEPTION_STATUS_MAP`/domain exception exports has a Spanish message; unknown code falls back to a generic message, never the raw string.
  Drift verified 2026-09-13: `step-messages.ts:11-22` knows 7 codes including phantom `GOOGLE_NOT_CONNECTED` (backend never emits it) and misses `NO_PROVIDER_CONNECTED`, `CUOTA_NUMERO_CONFLICT`, `CUENTA_MES_DUPLICADA`, `CHECKLIST_LINK_FAILED`; unknown falls back to the raw code (`:26`). Backend's full list (10): ACTIVIDADES_MISSING, NO_PROVIDER_CONNECTED, CHECKLIST_INCOMPLETE, COHERENCE_CHECK_FAILED, SECRET_DETECTED_IN_PACKAGE, PACKAGE_PENDIENTE, CUOTA_POSITION_CONFLICT, CUOTA_NUMERO_CONFLICT, CUENTA_MES_DUPLICADA, CHECKLIST_LINK_FAILED (`app/core/exceptions.py:8-40`). `app/cuentas-cobro/[id]/page.tsx:13-23` declares a local `CuentaCobro` type with `valor_periodo`/`numero_periodo`/period dates that don't exist on `CuentaCobroResponse` (`lib/cuentas-cobro-api.ts:45-68`). Also fix object-shaped `detail` rendering `[object Object]` in `extractApiError` (`lib/api.ts:117`, tracked 1.1 follow-up).
  Files: `components/stepper/step-messages.ts`, `app/cuentas-cobro/[id]/page.tsx`, `components/stepper/steps/step-2-cuota.tsx`, new `lib/backend-error-codes.ts` (checked-in list + a vitest that fails on any gap).
  **Done 2026-09-13.** Branch `feat/phase1-error-codes-ui`, 12 work-unit commits. Round 1 review (reliability): CONDITIONAL GO — BLOCKER: the regression net was a no-op (its `not.toBe(code)`/`not.toBe("")` assertions were satisfied by the new generic fallback; mutation-tested: deleting a code still passed 10/10) → now asserts `isKnownCode && !== GENERIC_CODE_MESSAGE`; CRITICAL: `[object Object]` still reachable via `{detail:[{foo:1}]}` and blank banner on `{detail:[]}`; CRITICAL: step-7 regenerate banner rendered the raw code and `ERROR: Network Error`; WARNING: axios English text leaked when `response` existed without `detail`. Round 2: GO — 3 WARNINGs fixed (prototype leak `codeMessage("constructor")`, step-7 dropped the backend `detail` → mirrored `errorFinding`, `contratos/[id]` silent failure on HTML 502). Also fixed on the way: `use-radicar-cuenta.ts` detected unknown codes via `codeMessage(code) !== code`, which the new fallback would have silently broken → `isKnownCode()`. Suite 584 → 617, `tsc` clean, build 15 pages. PR [#44](https://github.com/Juanpgm/cashing_frontend/pull/44) — **MERGED**.
  Follow-ups tracked: `app/contratos/[id]/page.tsx:49-56,496-499` has the same `CuentaCobro` type drift (→ 1.5b); `{detail:["a","b"]}` string arrays fall back to generic instead of joining. Third occurrence in this plan of "green test that verifies the wrong thing" — see Engram `pattern/test-verifies-wrong-thing`; every future "regression net" must be mutation-tested in the implementation prompt.
- [x] **1.5a** Exception mapping walks the MRO (`domain_to_http`, `app/core/exceptions.py:186-189` is an exact `type(exc)` dict lookup today).
  Edge cases: a new subclass of `ValidationError`/`NotFoundError` maps correctly without a code change; an unmapped root `DomainError` still yields 500; mapping order when a class inherits from two mapped bases.
  Files: `app/core/exceptions.py`, `tests/test_exceptions.py`.
  **Done 2026-09-13.** Branch `fix/phase1-exception-mro`, 1 commit. `domain_to_http` walks `type(exc).__mro__`, first `EXCEPTION_STATUS_MAP` hit wins, falls back to 500 only if nothing matches — pure superset of the old exact-match behavior. 17 new tests incl. a multi-inheritance case pinned via `mro.index()`. Review: GO, no findings — C3 linearization traced independently, call sites audited. Suite 2559 (+17). PR [#73](https://github.com/Juanpgm/cashing_backend/pull/73) — **MERGED**.
- [x] **1.5b** Replace the hand-rolled `err.detail` reads with `extractApiError` — **18 sites, not 3** (verified 2026-09-13): `app/contratos/page.tsx:98`, `app/integraciones/page.tsx:56`, `app/login/page.tsx:26,42`, `app/register/page.tsx:44`, `components/actividades-table.tsx:230,322,361`, `components/checklist-full-view.tsx:1039,1070,1651`, `components/editar-contrato-modal.tsx:152`, `components/evidencias-tab.tsx:487`, `components/generar-tab.tsx:129,585`, `components/google-test-panel.tsx:136,240,347`, `components/nuevo-contrato-modal.tsx:149`. Add an ESLint `no-restricted-syntax` rule banning `.detail` member reads outside `lib/api.ts`. Two PRs to stay under 400 lines: 1.5b-i (`app/**`), 1.5b-ii (`components/**`).
  Edge cases: `detail` as string / object / array / missing; network error with no response; each migrated site's existing vitest still passes.
  **Done 2026-09-13, plus a prerequisite `chore(lint)` PR.** This repo had NO ESLint at all — added it (`eslint.config.mjs`, `next/core-web-vitals`+`next/typescript`, the `.detail` guard rule, wired into `scripts/gate.mjs` as a changed-files-only hard gate; `next.config.ts`'s own build-time lint disabled since it has zero backlog tolerance) — PR [#49](https://github.com/Juanpgm/cashing_frontend/pull/49) **MERGED**. Rule proven live by reverting one fixed site and re-linting. Known false positives (out of scope): fires on `components/stepper/steps.ts`/`step-6-justificacion.tsx`'s unrelated domain `.detail` payload.
  1.5b-i (`app/**`, 4 sites): GO, real bug found (`app/contratos/page.tsx` read a top-level `err.detail` axios never populates, silently discarding real delete-failure messages), mutation-confirmed. PR [#50](https://github.com/Juanpgm/cashing_frontend/pull/50) — **MERGED**.
  1.5b-ii (`components/**`, 7 files): same bug class in `nuevo-contrato-modal.tsx`/`editar-contrato-modal.tsx`, mutation-confirmed. PR [#51](https://github.com/Juanpgm/cashing_frontend/pull/51) — **MERGED**.
  Tracked, not fixed: several migrated catch blocks (`generar-tab.tsx`, `google-test-panel.tsx`, 3 sites each in `checklist-full-view.tsx`/`actividades-table.tsx`, `evidencias-tab.tsx`'s generic branch) have no dedicated test asserting message content; `extractApiError`/`hasApiErrorDetail` have no unit test of their own, only indirect coverage.
- [x] **1.6** Agent escape-hatch tools: `marcar_requisito` (no_aplica / cumplido_manual / desvincular / observaciones), `obtener_estado_radicacion` (stepper-state), `generar_cuenta_cobro_pdf`, `editar_actividad`. Document the coherence gate in `radicar_cuenta`'s tool description.
  Edge cases: marking no-aplica on an enviada cuenta rejects; cross-user ownership returns 404, not a leak.
  Files: new `app/tools/catalog/*`, `app/api/v1/checklist.py` (reused), tool registry.
  **Done 2026-09-13.** Branch `feat/phase1-agent-escape-hatch-tools`, 1 commit. All 4 tools thin-wrap existing service functions, no logic duplicated. Real gap found: `checklist_service`'s mutators + the REST endpoint had zero estado gate (unlike every other cuenta_cobro mutation) — guarded at the tool layer only, deliberately. Cross-user ownership is `ForbiddenError` (403), not `NotFoundError` (404) as the plan assumed — confirmed pre-existing, test-locked codebase convention, not changed. Review (layer-inconsistency axis): CONDITIONAL GO — confirmed by mutation test the tool guard genuinely blocks, confirmed the service/REST/**frontend** paths remain fully ungated (worse than client-side-only: the frontend has no gate at all, and auto-fires a SECOP refresh on mount with no estado check), decided NOT to fix at the service layer here (would break the auto-fire on every already-radicada cuenta a user opens, cross-repo scope) — filed as a P1 follow-up instead. One test renamed to match its actual behavior. Suite 2599 (+22 from baseline). PR [#77](https://github.com/Juanpgm/cashing_backend/pull/77) — **MERGED**.
  **P1 follow-up tracked, not this slice**: gate `checklist_service`'s requisito-mutators + `PATCH /checklist/{codigo}` + the auto-vincular-on-mount path on BORRADOR/RECHAZADA, with a corresponding frontend read-only mode for `enviada` cuentas.
- [x] **1.7** `persistir_evidencias` by handle, not re-emission — takes `cuenta_id` + the `discovery_cache` key instead of the full `list[ObligacionJustificada]` payload.
  Edge cases: 10-obligación/40-link discovery persists in full; expired handle returns an actionable "rediscover" error; double-persist of the same handle doesn't duplicate rows.
  Files: `app/tools/catalog/evidencias.py`, `app/services/discovery_cache.py`, `app/services/evidence_persist_service.py`.
  **Done 2026-09-13.** Branch `feat/phase1-evidencias-handle`, 1 commit. Key finding: `descubrir_evidencias` is shared by 3 callers (agent, REST, MCP) — kept its return type stable, added `handle_id` additively; `persistir_evidencias` had no other caller, changed freely. New `evidence_handle_cache.py` (in-process dict, 20min TTL); token savings via `agent_chat_service`'s existing compact-serializer mechanism. Double-redeem re-runs the already-idempotent underlying persist, by design. Review (cross-user-isolation + multi-worker axis): GO — verified line-by-line, the exact handle-hijack attack tested and rejected; confirmed single-worker deployment makes the in-process cache correct today (flagged as a future risk only). Suite 2622 (+23). PR [#78](https://github.com/Juanpgm/cashing_backend/pull/78) — **MERGED**.
- [x] **1.8** Phase-gated tool set (drop SECOP/adiciones/plantillas unless relevant) + trimmed descriptions on the 4 largest tools; `asyncio.timeout` per turn; per-model timeout cut for the interactive loop; startup assertion that `LLM_PRODUCTION_FALLBACK_MODEL` is non-empty in production.
  Edge cases: prompt token count (measured via `to_openai_tools()`) drops below 6,000/iteration; model outage surfaces a message in <2 min, not ~12.
  Files: `app/services/agent_chat_service.py`, `app/adapters/llm/litellm_adapter.py`, `app/core/config.py`.
  **Done 2026-09-13.** Branch `feat/phase1-agent-timeouts-tool-trim`, 8 commits. Measured 10,659 → 5,604 tokens/iteration (fresh conversation); worst-case model-outage detection ~12min → 93s. **Round 1 review found a severe bug**: the cuenta-unlock was tool-name-based, so a user with an existing cuenta discovering it via `listar_cuentas_cobro` (the system-prompt-mandated path, never in the unlock list) got zero cuenta-scoped tools forever — plus a recap-amnesia bug that re-triggered it mid-flow. Fixed with evidence-based unlock (real `CuentaCobro`-shaped record detected by field shape in any tool's output) + a sticky recap line. **Round 2 re-review independently reproduced the original deadlock, confirmed the fix by execution and mutation testing**, checked all 36 tools' schemas for a false-positive-unlock collision (none), caught one narrow residual (sticky line itself crowded out of the recap budget under 3+ busy turns) — fixed. Suite 2686 (+64 over the whole slice, no regressions). PR [#79](https://github.com/Juanpgm/cashing_backend/pull/79) — **MERGED**.
- [x] **1.9** End-to-end playbook test: the documented 10-step prompt sequence driven through `chat_with_tools` with `LLM_PROVIDER=fake` + 0.6's outcome-aware script (drift verified 2026-09-13: `tests/test_agente_cadena_completa.py` has 2 tests that drive tools directly via `invoke_tool`, no `ScriptedLLM` — reuse the real seam instead of inventing a test-only one), completing inside `MAX_TOOL_ITERATIONS` (20, `agent_chat_service.py:69`) including `preparar_radicacion`. Merge the two informe tools; raise `MAX_CHAT_FILES` (5, `agent_chat.py:25`) to fit the 6 mandatory uploads in one turn.
  Edge cases: iteration cap hit mid-playbook — committed writes survive, recap allows continuation; malformed tool args don't get misread as a model mistake; failure on file 4/5 leaves no orphan stub activities.
  Files: `tests/test_agente_cadena_completa.py` (extend), `app/api/v1/agent_chat.py` (`MAX_CHAT_FILES`).
  **Done 2026-09-13. Last agent-tool slice.** Drives the real 10-step sequence through the real `chat_with_tools()` loop, `LLM_PROVIDER=fake`, across a realistic 4-turn conversation, ending in a real `radicar_cuenta` success. Did NOT merge the two informe tools (materially different, purpose-specific descriptions). `MAX_CHAT_FILES` 5→6. Review round 1: CONDITIONAL GO — confirmed 3 "bugs fixed" were fake-harness-only (0 production impact); confirmed the flagship test attached 9 files in one turn, which the real API's `MAX_CHAT_FILES=6` would reject — restructured into a realistic 4-turn conversation; corrected the "full playbook" framing to state plainly it verifies tool-calling mechanics, not Google-based evidence discovery/classification (inherently untestable via a deterministic fake LLM, covered only by `docs/prod-test-plan.md`'s manual layer). While restructuring, hit a live instance of 1.8's tracked recap-budget gap — resolved using 1.8's own sticky-recap mechanism correctly. Suite 2699 unchanged (test-only restructuring). PR [#80](https://github.com/Juanpgm/cashing_backend/pull/80) — **MERGED**.
  **Boundary of "agent working 100%" made explicit**: automated E2E solidly covers the agent's tool-calling reliability (the part that was actually broken — see 1.8's deadlock bug). Google-based evidence discovery/classification remains a manual-verification item per this repo's own doctrine, not an automatable gap in this slice.
- [x] **1.10** (NEW 2026-09-13) Local pre-merge gate that mirrors CI exactly while GitHub Actions is billing-locked: `cashing-backend-master/scripts/pre-merge.ps1` (`uv run python -m pytest --cov=app` ≥70%, ruff/mypy non-blocking) and frontend `npm run gate` (`npm run test:unit`, `npx tsc --noEmit`, `npm run build`, the mocked Playwright specs). Prints `GATE OK <sha> <counts>` to paste into every PR description. When billing is restored, `ci.yml` calls the same scripts.
  Edge cases: a failing step exits non-zero and never prints `GATE OK`; dirty working tree is reported in the summary; coverage below threshold fails.
  **Frontend half done 2026-09-13.** Branch `chore/phase1-local-gate`, 4 commits. Review (fail-open axis): CONDITIONAL GO — BLOCKER: without `CI=true` a stray `test.only` passed the gate (`forbidOnly` is CI-gated) → fixed, proved with a forced `GATE FAIL e2e`; BLOCKER: `reuseExistingServer` could certify a stale dev server → TCP probe on :3000 refuses; MAJOR: no `npm ci` equivalent → `npm ls --depth=0` step 0 + `--ci-install`; MAJOR: parsers swallowed `N failed` and took the first summary match → anchored, last match, failed/flaky/0-passed fatal (27 unit tests). Independently verified: `GATE OK 06623f3 unit=671 e2e=7 tsc=clean build=ok dirty=no node=25`. PR [#46](https://github.com/Juanpgm/cashing_frontend/pull/46) — **MERGED**.
  **Backend half done 2026-09-13.** Branch `chore/phase1-local-gate`, 3 commits. Fixed `scripts/test-postgres.sh`, broken since Postgres moved into the separate `cashing-infra` compose stack (additive-only `docker-compose.infra.test.yml` overlay resets the host-port publish the test flow never needed; `env_file: required: false` for a zero-`.env` checkout). Unplanned real bug found and fixed: `pytest-cov` was undeclared, so `--cov=app` (CI, `make test-cov`, this gate) was silently broken — dev-only dependency, production `requirements.txt` untouched, confirmed by review. `scripts/pre-merge.ps1` mirrors CI's pytest+coverage step (fatal) + ruff/mypy (reported). Review (dependency/production-safety axis): GO. Independently verified: 2503 passed, coverage 86.24%; `test-postgres.sh` proved live against real Postgres. PR [#71](https://github.com/Juanpgm/cashing_backend/pull/71) — **MERGED**. **Both halves of slice 1.10 done.**

**Phase 1 exit criteria**: a fresh local user radicates via the guided wizard without touching step 3's button; the full-playbook agent test is green.

## Phase 2 — Speed without breaking (repo: `cashing-backend-master`, strict order)

- [x] **2.1** Document generators (WeasyPrint, python-docx ×6, openpyxl, zip) → `asyncio.to_thread`.
  **Done 2026-09-13.** Branch `perf/phase2-async-doc-generators`, 5 commits. Re-audited the call-site list: one stale finding (already offloaded), one confirmed dead code (`template_filler.py`). Timing showed every generator except untestable-locally WeasyPrint completes in ms — none need 2.7's background-job pattern. Review: GO — no shared mutable state crosses the thread boundary, exception propagation unchanged, both audit corrections independently re-verified. Suite 2756 (+23). PR [#82](https://github.com/Juanpgm/cashing_backend/pull/82) — **MERGED**.
  Follow-up tracked, not fixed: unwrapped parsing/checklist-inference call sites in `pipeline.py`/`requisito_inference_service.py`/`document_service.py:463`, outside this slice's scope.
- [x] **2.2** Remove `_reload_contrato_response` / `_reload_cuenta_response` double-loads; build responses from the in-session object.
- [x] **2.3** Bulk-activity `flush`/`refresh` loops (4 sites) → `add_all` + one `flush`.
  **Done 2026-09-13, combined PR.** Branch `perf/phase2-reload-and-flush-cleanup`, 3 commits. Reload removed at 7 sites, kept at 3 genuinely-needed ones. Found and fixed 2 real regressions while implementing (selectin collection not populated on a transient object; `updated_at` not auto-refreshed on UPDATE). Review found a THIRD instance of the same bug class: monetary decimal scale silently lost at 3 write sites (`"1234567.8"` vs the DB-normalized `"1234567.80"`) — fixed with the same targeted-refresh pattern, plus dropped 3 banned `float()` conversions on money values. Contrato detail 19→11 queries. Suite 2774 (+18). PR [#83](https://github.com/Juanpgm/cashing_backend/pull/83) — **MERGED**.
- [x] **2.4a** Kill the `selectin` cascade → `raise_on_sql` on `Contrato.cuentas_cobro`, `Obligacion.actividades`, `CuentaCobro.borradores`.
  **Done 2026-09-14.** Branch `perf/phase2-4a-raise-on-sql`, 2 commits. Only the 3 relationship declarations plus one test file needed to change — zero production call sites accessed any of the three relationships implicitly, so no explicit `selectinload()` was needed anywhere in this slice (that hardening is 2.4b, deliberately deferred). One real test breakage in `test_agente_cadena_completa_chat_loop.py` (stale ORM access across an `await`), fixed by capturing the UUID once instead of re-touching the expired object; chasing it down surfaced an undocumented side effect of the old `selectin` cascade (accidental un-expiring of an unrelated identity-mapped object via `db.refresh`), now gone as expected. Review (opus, dangerous axis: "is 'zero call sites broke' actually true?"): **GO**, verified true via an exhaustive (not sample-check) `rg` sweep distinguishing real relationship accesses from same-named-but-unrelated attributes, dynamic `getattr`, and implicit Pydantic `from_attributes` reads. 3 non-blocking findings, 1 fixed same-branch (misleading comment in all 3 model files, corrected to point at the real `cobertura_service.py:76` example), 2 left as tracked SUGGESTIONs (test docstring wording; one query-budget test under-measures due to an empty-`Actividad` fixture). Suite 2792 (+0, comment-only final commit). PR [#85](https://github.com/Juanpgm/cashing_backend/pull/85) — **MERGED**.
- [x] **2.4b** Explicit `selectinload` at the ~12 call sites that need it (proactive hardening — none broke when the flag flipped in 2.4a, but future access patterns could hit `raise_on_sql`). Run under `make test-pg`.
  **Closed as no-op, 2026-09-14.** Dedicated audit sub-agent (sonnet, read-only, exhaustive `rg` sweep of `app/` + independent re-derivation from 2.4a's own findings) confirmed **zero** real production call sites read `Contrato.cuentas_cobro`, `CuentaCobro.borradores`, or `Obligacion.actividades` via attribute access at all — every caller queries the child model directly instead (e.g. `select(CuentaCobro).where(CuentaCobro.contrato_id == ...)`), a consistent codebase idiom. The plan's original "~12 call sites" estimate predates 2.4a's own exhaustive review and was simply wrong. Also independently re-verified the sibling `CuentaCobro.actividades` relationship (never flipped, still `lazy="selectin"`) — its 5 real reads are all correctly eager-loaded already; one harmless over-fetch found in `cruzar_service.py:259` (loaded but never read in that function), left as-is, out of scope. No forward-looking risk found (no TODOs/partial features near these relationships that would start using the attribute path). `tests/test_raise_on_sql_slice_2_4a.py` already provides the coverage this slice would have added. Nothing manufactured to justify the slice — genuinely no code changed.
- [x] **2.5** Cheap checklist: reuse `cuenta.contrato` instead of re-selecting it in `construir_checklist_completo`; memoize `listar_catalogo`; PATCH returns only the changed item.
  **Done 2026-09-13.** Branch `perf/phase2-cheap-checklist`, 1 commit. Contrato reuse verified against real `InstanceState.unloaded` semantics. Catalog memoized per-session (`db.info`, not process-wide — unsafe given how tests recreate schema). PATCH correctly scoped out: already narrow, frontend discards the body. Real regression found and fixed mid-implementation: the catalog cache broke `/refresh-secop`'s real rollback path, fixed with an `expired` check on every read. Checklist 47→36 queries, radicar 80→70. Review: GO, confirmed `expire_on_commit=False` makes commits safe, `db.expire_all()` never called anywhere, two-calls-same-session cache hit proven via real query counter. Suite 2782 (+8). PR [#84](https://github.com/Juanpgm/cashing_backend/pull/84) — **MERGED**.
- [x] **2.6** Batch evidence upload: one `IN` dedup query, `gather`+semaphore(8) uploads, one commit per batch, in-memory seen-set for intra-batch dedup; `subir_evidencias_desde_chat` passes `BackgroundTasks`; parallelize the per-obligación matcher/justify loops.
  **Done 2026-09-14.** Branch `perf/phase2-batch-evidence-upload`, 1 commit. Pre-audit confirmed the plan's scope was fully accurate (not stale) — every sub-item genuinely unimplemented, and the audit surfaced that the existing per-file-commit design was load-bearing for dedup correctness, meaning the in-memory seen-set is a correctness fix, not just polish. Batched `IN` dedup query, `gather`+`Semaphore(8)` bounded uploads, one-commit-per-batch + seen-set (I personally mutation-tested this by hand — disabled the seen-set, watched a real duplicate row get created, restored it, confirmed clean), `ToolContext.background_tasks` plumbed through the sync chat path only (SSE path deliberately excluded, verified via tracing its independent session's actual close timing, not assumed), matcher/justify loops parallelized via `asyncio.gather` after confirming exhaustively neither touches the shared DB session. Query counts measured 26→22 service-layer, first-ever endpoint budget pinned at 41.
  Review (opus, 2 full passes, axis: money/data-integrity-adjacent correctness): CONDITIONAL GO both times — core mechanics held up under exhaustive independent re-derivation, but 4 fixes required: unbounded LLM fan-out on both new `gather`s (same bug class this repo already fixed once for Gmail) — bounded with a semaphore + added a missing log line; 2 docstrings overstating "never orphan" as absolute — corrected to name the real crash-window limitation, documented as a wider-but-accepted trade for batch atomicity; the SSE-exclusion docstring named the wrong session as the reason — corrected to the actual mechanism; a real untested invariant (a flushed Actividad stub surviving a failed batch) — closed with an explicit rollback inside the service itself, not left to caller convention, with a new assertion that itself caught a genuine bug in my first fix attempt (a rollback expiring a test fixture object, the same `MissingGreenlet` gotcha this codebase has hit before) — mutation-tested twice by hand, once for the invariant, once for my own bug in the test. Cross-request dedup race and SSE-path's still-inline classification tracked as explicit accepted follow-ups. Suite 2801→2814 (+13), verified independently before AND after every fix round, survived a git-stash round-trip during a mypy-baseline check (recovered correctly via `apply <sha>` + `drop`, not `pop`). Postgres (Docker): all dedup/concurrency/BackgroundTasks/matcher/justify tests green. PR [#88](https://github.com/Juanpgm/cashing_backend/pull/88) — **MERGED**.
- [x] **2.7** Package/report generation as a poll-based background job (mirrors the existing `ClasificacionJob` pattern), lock per cuenta.
  **Done 2026-09-14 (backend), paired frontend copy fix same day.** Branch `perf/phase2-package-job`, 1 commit. New `PaqueteJob` row per cuenta (migration `042_paquete_job.py`) mirroring `ClasificacionEvidenciasJob`'s exact pattern (`SELECT ... FOR UPDATE` re-select, staleness-window idempotent no-op, phantom-insert retry-once, `BackgroundTasks` reusing the request's own session). Option B (additive): existing `POST /paquete/regenerar` keeps its exact 200 response shape for the normal case, now routed through the same per-cuenta lock as two new endpoints (`POST /paquete/regenerar-async` 202, `GET /paquete/job` poll); a sync caller that loses the lock race gets a new 409 `PaqueteGenerationInProgressError` instead of silently duplicating the pipeline or blocking.
  Review (opus, 2 rounds). **Round 1** — CONDITIONAL GO: a real BLOCKER (SQLAlchemy's dirty-checking emits no UPDATE when an attribute is reassigned to a value it already holds — `_upsert_job`'s stale-job reset branch hit this exactly for a stale-`pending` row, so `updated_at` never bumped and a second concurrent caller would ALSO "win" the lock; proven empirically by the reviewer with a reproduction script) fixed by forcing `job.updated_at = datetime.now(UTC)` explicitly, plus a new regression test proving a second caller correctly sees the reset row as fresh (mutation-tested by hand: reverted, confirmed genuine failure, restored, confirmed green); a real CRITICAL (`PAQUETE_JOB_STALE_SECONDS`=120s was shorter than the pipeline's own real worst case — the frontend's `PAQUETE_REGENERAR_TIMEOUT_MS` already established ~300s) fixed by raising it to 300; a merge condition (frontend `CODE_MESSAGES` missing the new 409 code, backend `detail` leaking a raw `cuenta_id` UUID + an internal endpoint path) fixed on both sides, frontend as a paired PR. **Round 2** (fresh context, read-only) independently re-proved the BLOCKER fix with its own empirical probe across stale-PENDING/RUNNING/FAILED scenarios, independently mutation-tested the new regression test with its own scratchpad plugin, exhaustively grepped for stale references to the old 120s value or old message text (none), traced the full frontend rendering chain end to end. **Verdict: GO.** One cheap cosmetic fix applied same-commit: made the backend's `detail` byte-identical to the frontend's translated message so the UI's existing dedup guard collapses the redundant line instead of stacking two near-identical sentences.
  Non-blocking follow-ups tracked, not fixed (update 2026-09-19: the sibling `evidence_classification_service._upsert_job` no-op-reassignment bug named first below was fixed by 5.5, PR #95): the identical latent no-op-reassignment bug exists in the already-merged sibling `evidence_classification_service._upsert_job`'s reset branch; `updated_at` is stamped once at the `running` transition, never heartbeat-refreshed, so a single LLM call could still legitimately outlive 300s under a full provider-fallback cascade; the crash-recovery availability window widened 120s→300s as an accepted consequence of the CRITICAL fix; a third, still-unlocked writer to the same storage key via `app/tools/catalog/paquete.py`'s agent tool (more severe than the module's own documented KNOWN GAP); minor `sa.JSON()` vs `postgresql.JSONB` migration style divergence.
  Suite 2814→2825 (+11: 10 from the slice, +1 the BLOCKER regression test). Postgres/production caller-sweep clean. PR [#89](https://github.com/Juanpgm/cashing_backend/pull/89) — **MERGED**. Paired frontend fix (`cashing-frontend`, branch `fix/paquete-generacion-en-curso-copy`, 1 commit, `lib/backend-error-codes.ts` + `components/stepper/step-messages.ts`): added `PAQUETE_GENERACION_EN_CURSO` to the checked-in code list and its Spanish translation. No further frontend wiring needed — `step-7-paquete.tsx`'s existing `codeMessage(extractApiErrorCode(...))` call already translates any code generically for the existing sync path; traced end to end (`domain_error_handler` → `extractApiErrorCode` → `codeMessage` → render). Verified: 30/30 vitest, `tsc --noEmit` clean. PR [#62](https://github.com/Juanpgm/cashing_frontend/pull/62) — **MERGED**. The new async trigger+poll endpoints are not yet wired into the frontend wizard — that's slice 3.5's job (needs 2.7, per the plan's own stated dependency).
- [x] **2.8** Shared `httpx.AsyncClient` per process (SECOP/Graph/Wompi); frontend: `refetchOnWindowFocus: false`, lazy per-row `usePaquete`, de-collided query keys.
  **Done 2026-09-15.** Two audits first resolved stale plan wording: query-key "collisions" turned out not to exist (every key already namespaced, confirmed via exhaustive sweep — sub-item dropped as moot, not implemented); "lazy per-row `usePaquete`" via the obvious `enabled: expanded` copy-paste would have broken the button (it's always-visible, not expand-gated) — replaced with a new visibility-based `useInView` hook instead; folding paquete status into a batch/JOIN list endpoint was investigated and rejected (the live per-row data — a storage HEAD call + live obligación recomputation — has no cheap DB-only equivalent without accepting a stale snapshot).
  Backend (`perf/phase2-8-shared-httpx-client`, 1 commit): new `app/core/http_clients.py`, `get_shared_client(name, **kwargs)` — a lazy, event-loop-aware cache (`WeakKeyDictionary` keyed by the loop object, not `id(loop)`, to avoid CPython's id-reuse-after-GC hazard) reused instead of a fresh client per call. Deliberately NOT a module-level singleton (the `langfuse_client.py` pattern) — this codebase already learned this exact lesson once for a pooled DB connection (`tests/conftest.py`'s asyncpg/`NullPool` fix for pytest-asyncio's per-test event loops). Converted 10 call sites across SECOP/Graph/Wompi. Review (opus, axis: production correctness for money/legal-filing-adjacent integrations) verdict GO, empirically verified (not just reasoned): no race in the cache (synchronous body, no `await` gap), retry loops can never retry onto a connection already proven healthy, merged cache keys confirmed byte-identical kwargs, pool sizing confirmed adequate, the `WeakKeyDictionary` safety claim verified with a standalone script, retargeted tests confirmed non-decorative. One real finding fixed same-round: the shared `"graph-token"` client would have leaked OAuth cookies across different users — reverted to a fresh per-call client there (no real perf case for sharing a low-frequency OAuth endpoint anyway). Suite 2825→2832 (+7). PR [#90](https://github.com/Juanpgm/cashing_backend/pull/90) — **MERGED**.
  Frontend (`perf/phase2-8-query-hygiene`, 1 commit): `refetchOnWindowFocus: false` globally; new `lib/use-in-view.ts` (`useInView`, IntersectionObserver-based, reveal-once-stay-revealed, falls back to immediately-visible when unsupported) gates the cuentas-cobro list row's `usePaquete` call. Review (opus, axis: silent staleness for a money-adjacent download gate) verdict GO, traced against `@tanstack/query-core` source directly: confirmed the "reveal once" `enabled` gate does not suppress a later `invalidateQueries`, confirmed row component identity survives list re-renders (no defeat via remount), confirmed no query anywhere relied on focus-refetch as its only staleness-recovery path. Two review suggestions applied same-commit: `rootMargin: "200px"` (avoids a visible wave of falsely-grey buttons while scrolling) and a new regression test locking in the invalidation-after-reveal behavior (personally mutation-tested: a temporarily mutated query-key shape made it genuinely fail, restored, confirmed green). Suite 969→985 (+16). PR [#63](https://github.com/Juanpgm/cashing_frontend/pull/63) — **MERGED**.
  Non-blocking follow-ups tracked: Wompi's refactored client code has no direct test coverage (mocked at the function boundary); pool exhaustion above ~100 concurrent shared-client requests degrades to a clean error rather than unbounded concurrency (unreachable at current scale); `useInView`'s "latest ref" pattern is technically impure per React's concurrent-rendering guidance but inert given its only call site.

Each slice's edge cases (see published plan §2 table for the full list): concurrent regeneration converges to one result and a lock, not two; a batch failure at item N leaves nothing partially committed and no orphaned storage object; `raise_on_sql` failures caught by the full suite, not by production.

**Phase 2 exit criteria**: query-count budgets from 0.2 green in CI; zero document generation on the event loop; local smoke against Neon dev shows checklist load under 1s.

## Phase 3 — Three-screen experience (repo: `cashing-frontend`, builds on `radicacion-stepper`'s shell)

- [x] **3.1** Split `checklist-full-view.tsx` (1727 LOC) and `stepper-shell.tsx` (925 LOC) into named sub-components, no behavior change (vitest suite as the regression net).
  **Done 2026-09-14.** Branch `refactor/phase3-split-large-components`, 1 commit. `checklist-full-view.tsx` 1672→421 lines: its own already-separate function components relocated to `components/checklist/` (file relocation + export, no JSX/prop restructuring). `stepper-shell.tsx` 983→657 lines: split along state-ownership boundaries into named hooks under `components/stepper/hooks/` plus 2 pure presentational components — the riskier half, a regrouping of `useState`/`useEffect` calls, not a mechanical JSX split. Two near-misses caught and fixed by the implementer DURING extraction, before shipping (a hand-rolled badge instead of reusing `Pill`; a dropped `setDescartarError(null)` call). Verified beyond vitest: 27 real Playwright tests run against the live local backend (not mocked) — the full wizard journey, checklist upload/evidence/classification, mobile, a11y, and the exact `agent-dock-footer-overlap.spec.ts` regression that measures `stepper-shell.tsx`'s DOM at 4 real viewports — all green. Adversarial review (opus, axis: prove zero behavior change) did a mechanical multiset-diff + in-order-subsequence check of every extracted file against the original, traced every dependency array, and found the hook split changed the effect flush order — verified exhaustively unobservable today, documented as a latent trap in the module docstring rather than left silent. GO, no BLOCKER/CRITICAL; 1 WARNING (the effect-order note) + 3 cosmetic stale-doc-comment SUGGESTIONs, all fixed directly post-review (trivial, zero-risk). Incidental: fixed a pre-existing unrelated ESLint error the branch's own touch to `step-7-paquete.tsx` would otherwise have turned blocking under the changed-file-scoped gate. Suite 882/882 unchanged (pure refactor, zero new tests needed). PR [#56](https://github.com/Juanpgm/cashing_frontend/pull/56) — **MERGED**.
- [x] **3.2a** (NEW 2026-09-13) Consumer audit before retiring anything: every link/`router.push`/spec that reaches `app/cuentas-cobro/[id]/radicacion` (incl. `app/contratos/[id]/page.tsx:134-144` which sends NEW cuentas there) and `app/cuentas-cobro/[id]`, plus what `GenerarTab` (Analizar → Semáforo → Generar, per-file downloads) offers that the wizard does not. **Decision gate for the user**: retire and accept the loss (~1 slice) vs. port that flow into the wizard first (~2 extra slices). Ask with the audit in hand.
  **Done 2026-09-14 (audit) + decision made.** Audit (Explore sub-agent, read-only) found the picture is more nuanced than a clean binary: `GenerarTab` has 4 things the wizard lacks — a manual "Analizar documentos" (`cruzar`) re-analysis trigger with a destructive-replace warning, plus 3 standalone downloads (informe de actividades, informe de supervisión, carpeta de evidencias). The 3 downloads are a small, low-risk addition to step 7 (same `descargarInforme`/gated-button pattern already there). The `cruzar` trigger is genuinely expensive — no existing wizard step is a clean home for it without duplicating step 6's actividad-generation action or complicating step 4's passive-coverage design; would need a new sub-step. Also found: the legacy page is the ONLY way today to view an already-radicada (aprobada/pagada) cuenta's history — `AccionesCuenta` already correctly routes only editable-state cuentas to it via a dead-end (see below), immutable-state cuentas correctly get the legacy page. Also found a real routing bug independent of the decision: `app/contratos/[id]/page.tsx` has ZERO references to `/radicar` anywhere — it ALWAYS sends new cuentas and the "Checklist y documentos" icon to the legacy page regardless of estado, unlike `app/cuentas-cobro/page.tsx`'s `AccionesCuenta` which already differentiates correctly. Also flagged 2 possibly-orphaned, uncovered-by-tests affordances only on the plain `[id]` detail page ("Generar con IA" agent button, "Ver borrador" HTML preview) — called from nowhere else in the codebase, unclear if still live/needed, not part of this decision.
  **User decision** (asked with the audit in hand, per AskUserQuestion): **port the 3 cheap downloads into wizard step 7; defer the `cruzar`/semáforo re-analysis trigger as a tracked, explicitly deferred gap (not built now); keep the legacy page alive ONLY for its historical-view role** (aprobada/pagada cuentas — unchanged from today); **retire the legacy page as an entry point for in-progress (borrador/rechazada) cuentas**, since the wizard already fully covers that case once the routing bug above is fixed. This refines 3.2's scope below.
- [x] **3.2** One entry point: remove `RadicarModoModal` and the contract-detail "Nueva cuenta de cobro" shortcut; redirect the two legacy cuenta-detail routes to `/radicar/{id}`.
  **Refined scope per 3.2a's decision** (not a full retire): fix `app/contratos/[id]/page.tsx`'s cuenta-creation redirect and "Checklist y documentos" icon to route in-progress (`borrador`/`rechazada`) cuentas to `/radicar/{id}` instead of the legacy `/radicacion` page — matching `app/cuentas-cobro/page.tsx`'s `AccionesCuenta`, which already gets this right; leave `AccionesCuenta`'s existing aprobada/pagada → legacy-page routing untouched (that's the intended historical-view path, not a bug); add the 3 standalone downloads (`actividades`/`supervision`/`evidencias`) to wizard step 7 (`step-7-paquete.tsx`), mirroring `generar-tab.tsx`'s existing `ready`/`blockedReason` gating logic. **Deferred, NOT in this slice's scope**: the `cruzar`/semáforo manual re-analysis trigger — tracked as an explicit backlog item, needs its own future design/slice.
  **Done 2026-09-14.** Branch `feat/phase3-one-entry-point`, 1 commit. Removed `RadicarModoModal` entirely (deleted, zero live importers left — redundant now that slice 3.10's `AgentDock` puts chat inside the wizard itself) and the contract-detail inline "Nueva cuenta de cobro" creation modal (both now navigate straight to `/radicar?contratoId={id}`). "Checklist y documentos" icon now branches by `estado`, mirroring `AccionesCuenta` exactly; `enviada` gets a locally-duplicated reabrir-then-edit flow (not a dead end) since the plain cuenta-detail page has no reabrir affordance of its own. Ported the 3 downloads into step 7. Review (opus, axis: real behavior change with a duplicated code path, not a refactor): GO — traced the duplicated reabrir mutation's query-invalidation key precisely (correctly adapted, not blindly copied), proved the aprobada/pagada historical path byte-identical from the diff. 4 non-blocking findings fixed same-commit: missing dashboard-list cache invalidation on reabrir; now-orphaned `RadicarModoModal` file deleted; step 7's 2 new queries didn't distinguish loading/error from a genuine "zero actividades", misreporting network failures — fixed; missing reabrir-failure test — added. Also independently found (not by the implementer) and fixed: an e2e spec asserted the removed modal's UI — reproduced against the real local backend, updated to assert direct navigation, verified all 12 tests in that file plus 17 across both affected journey specs pass for real. Suite 882→899 (+17). PR [#57](https://github.com/Juanpgm/cashing_frontend/pull/57) — **MERGED**.
- [x] **3.3** Screen 1 "Tu cuota": pre-filled summary + Editar disclosure; advanced fields hidden; checklist defaults to standard with opt-in inference + diff + per-row undo.
  **Done 2026-09-15.** Branch `feat/phase3-3-screen-1-tu-cuota`, 1 commit. Scope confirmed with the user before implementing (3-question AskUserQuestion, given a fresh audit found no `design.md` exists anywhere — the plan's one-line bullets are the entire written spec): Screen 1 = today's existing group 1 "Preparar" (contrato + cuota) exactly, unchanged group boundary — reuses existing group infrastructure rather than restructuring it; the checklist-**mode** decision (not the full ongoing requisito list, which stays on Screen 2/3.4) relocates onto Screen 1, purely client-side (backend step-numbering untouched), using the existing `requisitos_definidos` flag as its completion signal; advanced fields hidden = `numero_cuota` + `fecha_transaccion` (informe_final stays visible, higher-stakes — drives `CUOTA_POSITION_CONFLICT` if set wrong).
  `Step2CuotaResumen` gained a `CollapsibleSubStep`-based "Editar" disclosure (closed by default) around `numero_cuota`/`fecha_transaccion`; `informe_final` added to the always-visible summary. New `ChecklistModoSection` inside the same component renders the (extended, not duplicated) `DefinirChecklistGate` when `!requisitos_definidos`, or a compact confirmation once resolved — reuses the exact `["checklist", cuentaId]` key `ChecklistFullView` already uses, needing zero changes there. `DefinirChecklistGate` extended: default tab flipped `"archivo"`→`"estandar"` (audit found today's default was the literal opposite of "opt-in inference" — inference was the path of least resistance) and reordered; new 2-state diff pill (`"nuevo"`/`"coincide"` — documented as 2-state not 3-state since the API returns no standard-catalog shape to diff against); new per-row undo (removed rows restorable, not dropped for good).
  Review (opus, 2 rounds, axis: data loss/incorrect submission for a money-and-filing-adjacent form). **Round 1**: GO on the slice's own scope, but empirically proved 2 real CRITICAL data-loss bugs — pre-existing on `master`, not introduced by this slice, but in the exact file/feature area being touched, fixed same-commit: a manually-added, named checklist row was silently absent from the actual submit payload (`emptyItem()` sets `codigo: ""`, no codigo input exists anywhere in the UI, and the backend silently skips any empty-codigo item, no error) — fixed with `withDerivedCodigos()`, deriving an UPPER_SNAKE code from the label (mirroring the backend's own inference-preview slug helper's shape), deduped case-insensitively against the whole batch; the same root cause left "Aplicar checklist" permanently disabled after a zero-item inference result even once a row was named — now recoverable. **Round 2** (narrow, fresh context) re-verified the fix independently (edge cases empirically traced: whitespace-only/emoji-only labels, real Spanish accent characters, never-mutates-an-existing-codigo) and found one more real gap: the derived codigo had no length cap, but the backend column is `VARCHAR(50)` with no truncation/validation on this endpoint — a plausible real requisito name (60+ chars) would 500 the ENTIRE batch on real Postgres/Neon, invisible on local SQLite. Fixed same-commit: capped at 50 chars matching the backend's own cap; also closed a coverage gap the round-2 review found (a mutation ignoring already-inferred rows' codigos when deduping survived every existing test) with a new dedicated test. Every fix personally mutation-tested by hand across both rounds (revert → genuine failure confirmed → restore → green), four separate times.
  Non-blocking follow-ups tracked: the diff pill's "coincide" label can't reflect whether a mapped row's content diverges from the standard baseline (no API data to compute a true 3-state diff); "Volver" discards preview edits + the undo buffer with no confirmation (pre-existing, out of scope); the new checklist query firing on every Screen-1 visit confirmed to be a harmless cache prefetch, not a duplicate fetch (groups 1/2 never co-mounted, shared 60s staleTime). Playwright: verified via exhaustive `rg` sweep (no live backend booted this session) that nothing in `e2e/**` is stale today — every spec pre-seeds the checklist via API before reaching the wizard UI; flagged that a *future* spec reaching group 1 unseeded would newly exercise the real gate UI there for the first time. Suite 985→1007 (+22). PR [#64](https://github.com/Juanpgm/cashing_frontend/pull/64) — **MERGED**.
  **A. "Editar" disclosure** (`Step2CuotaResumen`): `numero_cuota` (now read-only informational — no post-creation edit affordance existed before this either, VISIBILITY changed, not mutability) and `fecha_transaccion` (same debounced-autosave input, relocated verbatim) collapsed behind a `CollapsibleSubStep`-based "Editar" disclosure, closed by default (`defaultOpen={false}`, not driven by `complete` — this is a decluttering toggle, not a completion signal, per that prop's own documented use case). `informe_final` added to the always-visible summary `<dl>` (it wasn't rendered in Resumen mode at all before) — kept OUTSIDE the disclosure per the task's explicit higher-stakes rationale (`CUOTA_POSITION_CONFLICT`).
  **B. Checklist-mode decision relocated to group 1**: new `ChecklistModoSection` (same file), mounted below the cuota summary card in `Step2CuotaResumen` only (does NOT touch `GROUPS`'s member count/`Group`/`Step` contract — a purely client-side addition inside step 2's existing body, "shown once the cuota is confirmed" because `Step2CuotaResumen` itself only renders once the cuota exists). Reuses the exact `["checklist", cuentaId]` query key `ChecklistFullView`/`Step3Checklist` already use, so `DefinirChecklistGate`'s `onDone` invalidation (and any background refetch anywhere else in the app) is picked up here too, without new plumbing — confirmed `checklist-full-view.tsx`'s own `!data.requisitos_definidos` gate needed zero changes (verified by reading it, not assumed). Renders nothing while the query is loading (avoids a flash of the wrong state on an already-resolved cuenta), the real `DefinirChecklistGate` when `!requisitos_definidos`, or a compact confirmation card when `requisitos_definidos === true`.
  **`DefinirChecklistGate` extended (not duplicated)**: default tab flipped `"archivo"` → `"estandar"` (regression-locked by its own test) and reordered (estándar, archivo, texto) — inference is now genuinely opt-in. **Diff view**: 2-state (`nuevo` vs `coincide`), NOT 3-state — documented design decision (`diffStatus`'s own comment): `mapea_a_estandar` is the only signal `inferirRequisitosArchivo`/`inferirRequisitosTexto` actually return; no standard-catalog item shape is returned to diff `obligatorio`/keywords against, so "modificado" isn't computable from what the API gives. Replaces the old bare code badge with an explicit diff pill. **Per-row undo**: `removeItem` now moves the row into a `removed` buffer (with its original index) instead of dropping it; a "Quitados — podés deshacer" list renders per-row "Deshacer" buttons that splice the item back into its original (or best-effort clamped) position; buffer clears on a fresh inference run or "Volver" — genuinely restorable, not just visually hidden while still submitted (verified: the removed item is provably absent from the actual `definirRequisitos` payload). **Malformed/empty inference defensive fix**: `applyInferido` now guards `res.requisitos`/`res.avisos` with `Array.isArray` before `setState` — the pre-existing code would crash with `Cannot read properties of undefined (reading 'filter')` on a malformed 200 response (reproduced this as a genuine RED failure before fixing it, not assumed).
  Tests: 17 new (10 in `step-2-cuota.test.tsx` — editar-disclosure default-closed/reveal/no-reset-on-rerender/read-only-numero, informe_final always-visible incl. cuenta-query-resolves case, autosave-still-works-once-revealed, and 4 checklist-mode-section cases incl. the no-flash-on-already-resolved and background-refetch-flip edge cases; 7 in `definir-checklist-gate.test.tsx` — default-tab regression lock, tab order, diff view with a realistic mixed fixture, per-row undo incl. same-position restore and payload-exclusion, 2 malformed/empty-result crash-guard cases). 2 pre-existing tests adjusted to the new default tab (dropzone a11y test now clicks "Subir documento" first) — not weakened, just re-pointed at the tab that moved. Full suite 985 → 1002 (+17), all green. `tsc --noEmit` clean. `eslint` clean on all 4 changed files.
  **Real gotcha hit and fixed during implementation** (documented, not left for the next session to rediscover): `vi.useFakeTimers()` called before any `findByTestId`/`waitFor` silently hangs the WHOLE remaining test file — `@testing-library/dom`'s internal polling uses real `setInterval`, which under fake timers never fires, and a test-level timeout kills the test's own promise chain before its `finally { vi.useRealTimers() }` can ever run, leaving fake timers stuck for every later test. Fixed by only enabling fake timers immediately around the debounce-advance step, after all real-timer-dependent RTL queries settle.
  **Playwright staleness check** (could not run Playwright locally — no live backend booted in this session; verified via exhaustive `rg` sweep of `e2e/**` instead, per the task's own explicit fallback): **nothing is stale**. Every journey/edge-case spec that needs a defined checklist seeds it via a direct `POST /cuentas-cobro/{id}/requisitos` API call BEFORE visiting the wizard UI (`e2e/helpers/seed.ts`'s `seedChecklist`) — none drive `DefinirChecklistGate`'s tabs through the UI, so the default-tab flip is inert for existing specs. The only `step-2-cuota-*` testids referenced in `e2e/**` (`visitarGrupoPreparar` in `radicacion-paquete-completo.spec.ts`/`diez-ejercicios-radicacion.spec.ts`, plus `movil-390px.spec.ts`) only assert `step-2-cuota-resumen`/`step-2-cuota-crear` visibility, never `numero_cuota`/`fecha_transaccion`/informe_final content — untouched by the Editar-disclosure relocation. **New, flagged for awareness** (not a break, a genuine new surface): because every current spec pre-seeds the checklist before reaching the wizard, `ChecklistModoSection` always renders its compact confirmation in existing runs — but a FUTURE spec that reaches group 1 before seeding `requisitos` would now see the real `DefinirChecklistGate` inline there for the first time; worth a deliberate look if/when such a spec is added.
  Not attempted in this batch (explicitly out of scope, no PR opened, no push): 3.4 and 3.5 (next screens), any Playwright execution.
- [x] **3.4** Screen 2 "Completá tu cuota": single list, one chip + one primary action per row; parallel background prep with visible staged progress (reuses `ProgresoEtapas`); plantillas stay in the wizard.
  **Done 2026-09-15.** Branch `feat/phase3-4-screen-2-completa-tu-cuota`, 10 commits, PR [#65](https://github.com/Juanpgm/cashing_frontend/pull/65) — **MERGED** (squash `1ef7b14`). Session resumed from an uncommitted working tree (audited independently before committing: tsc clean, 1044 tests, 0 stale e2e selectors). **Validation found real defects in 3 layers**: (1) **live Playwright** (13 spec files, backend `LLM_PROVIDER=fake`, 41/43 → 43/43 after fix): the new wrapper around `Quitar` + the add-obligación `<select>` in `FilaEnlace` made the select take its longest option's width (a full obligación text), overflowing LEFT past the dialog so `Quitar` rendered outside the panel and the backdrop intercepted clicks — deterministic with realistic data, fixed with `min-w-0 max-w-[10rem]` + wrap (same constraint applied to `FilaSinClasificar`); (2) **review r1** (opus, CONDITIONAL GO): C1 intra-group `onNavigate(5)`/`(4)` was a no-op that scrolled to the TOP of the ~6000px screen because `switchToGroup` short-circuits on a same-group target — the slice's own test had codified the no-op as correct; fixed with per-member `#step-section-{step}` scroll+focus, guard-aware, with a group-path fallback; C2 step-5 post-upload `resultAvisos` hidden inside collapsed "Detalles" (moved out; also fixed a real crash on non-array avisos); W1 progressbar without accessible name (WCAG A); W2 mutation `<3→<4` at `use-group-auto-advance.ts:71` survived the suite (dedicated hook test added, now RED); W3 collapsing unmounted `ConsecutivoDsField` (kept mounted via `hidden`); (3) **review r2** (opus, narrow, 6 mutations): no functional defect in the r1 fixes, but W3's own mechanism opened a coverage hole (`hidden={false}` survived) — closed with `not.toBeVisible()` assertions, mutation-tested by hand. The 4 pre-existing `.detail` lint false positives in `steps.ts`/`step-6-justificacion.tsx` (blocking `npm run gate` on any branch touching those files) exempted per-line with justification. Suite 1007 → 1062 (+55). `GATE OK 5c6f140 unit=1062 e2e=7 tsc=clean build=ok dirty=no`.
  **Follow-ups tracked** (update 2026-09-19: the height decision is still OPEN and is the user's — see 5.3; the progressbar-name item below was closed by 5.6): UX decision on group 2's height (~5800-7100px in a ~373px scroll band under `revealMode:"all"` — collapse members by default / accordion / keep); no e2e drives "Subir formato de la entidad" → step 5 (C1 covered by jsdom + mocked `scrollIntoView` only); no layout assertion that `Quitar` stays inside the dialog at 390px; no screen-reader announcement of classification completion after removing `aria-live`; `FilaSinClasificar` still has 2 always-visible actions; axe never reaches a rendered `ClasificacionYSemaforo`.
  **Original implementation notes (pre-review)** — scope confirmed with the user beforehand via 3 explicit decisions before implementing: (1) unified STYLE not a data-model fusion — each of the 4 domains keeps its own component, only the `requisito-row.tsx` row idiom (collapsed default, chip, one primary action, "Detalles" toggle) is extended to them; (2) plantillas explicitly **stay in the wizard** (a "contract settings" page doesn't exist and is out of scope — this line item's own wording, "plantillas move to contract settings," is superseded by that explicit decision, not followed literally); (3) progress feedback uses REAL numbers where the backend already returns them (step 4's classification job) and `ProgresoEtapas`'s synthetic staged UI everywhere else (step 6).
  **Group merge (`components/stepper/steps.ts`)**: `GROUPS` 4→3 — `GROUPS[1]` ("Evidenciar", `members:[3,4]`) and `GROUPS[2]` ("Formalizar", `members:[5,6]`) merged into one `{group:2, label:"Completá tu cuota", members:[3,4,5,6]}`. `Group` type `1|2|3|4`→`1|2|3`; `clampGroup` `>4→4` became `>3→3` (a stale `4` now resolves to the new last group, not undefined behavior). New `GroupMeta.revealMode: "progressive" | "all"` field: group 1 and group 3 stay `"progressive"` (unchanged behavior); group 2 is `"all"` — a real UX call, documented in both `steps.ts` and `stepper-shell.tsx`'s doc comments: the 4 merged domains are loosely-coupled and each already handles its own missing-prerequisite state (step 5 is explicitly non-blocking by contract; step 6 has its own "sin evidencias" banner + jump CTA), so gating them behind sequential completion would hide legitimately-reachable work instead of protecting anything — chosen over a tabs UI to keep the "single list, single screen" spirit of this task's own title. `renderGroupMembers` (`stepper-shell.tsx`) now branches on `revealMode`.
  **Superseded 2026-09-16 by 5.3 (PR #68):** group 2 no longer ships `revealMode: "all"` nor `members: [3,4,5,6]`; on `master` it is `revealMode: "staged"`, `members: [4, 6, 3, 5]`, `stagedOrder: [CONTEXTO_MEMBER, 4, 6, 3, 5]` (`components/stepper/steps.ts:244-246`). The `"all"` mode remains a valid `GroupMeta.revealMode` value but no group uses it.
  `stepper-shell.tsx`: 7 literal `activeGroup === 4`/`< 4` comparisons found by exhaustive `rg` (matching the audit's "at least 7" — confirmed exactly 7, all fixed to `3`), PLUS one 8th site the audit's list did not name: `use-group-auto-advance.ts:71`'s `activeGroup < 4` (a separate hook file, not `stepper-shell.tsx` itself) — found by re-deriving the search myself rather than trusting the given list, fixed too. `step-progress-trail.tsx` and `member-section.tsx` needed ZERO logic changes (already generic over `GROUPS.length`/`members: Step[]`, confirmed by reading, not assumed) — only their stale doc comments (mentioning "4 group dots"/"group 4/Entregar") were corrected.
  **Row-style extension**: `step-4-evidencias.tsx`'s `FilaEnlace` (a linked-evidencia row inside an obligación group — used to show 2-3 always-visible actions: Confirmar/Quitar/agregar-obligación select) now collapses by default, chip + "Confirmar" (when proposed) visible, "Quitar"+select behind a new `detalles-{evId}-{obId}` toggle. `step-5-formato.tsx`'s `PlantillaCard` (used to show "Reemplazar"/"Subir plantilla" AND "Re-ejecutar detección" both always-visible, plus an always-open campos-detectados `<details>` and Consecutivo DS field) now collapses similarly behind a new `step-5-detalles-{tipo}` toggle, one primary upload/replace action left visible; `uploadError` stays outside the toggle (errors never hidden, matching `RequisitoRow`'s `patchError` convention). `FilaSinClasificar` (step 4's "Sin clasificar" bucket row) was deliberately LEFT AS-IS — also technically has 2 always-visible actions (select + "No aplica"), but touching it was out of scope for this batch to limit e2e blast radius; flagged as a real, non-blocking gap for a future slice.
  **Step 4 real progress bar**: new `components/ui/progress-bar.tsx` (`ProgressBar`) — an honest, real-fraction bar (`value`/`total` props, defensive zero-total guard, no synthetic curve), deliberately NOT `ProgresoEtapas` (documented in both components' doc comments: reusing the synthetic time-based curve here would DOWNGRADE information quality since the backend already returns exact `procesadas`/`total`). Wired into `ClasificacionYSemaforo`, `data-testid="clasificacion-progreso"` and its exact text format preserved verbatim (the live `evidencias-clasificacion.spec.ts` asserts against it via `toHaveText`).
  **Step 6 `ProgresoEtapas` wiring + parallelization investigation**: wrapped `generarJustificacionesMut.isPending` in `ProgresoEtapas` (`ETAPAS_JUSTIFICACIONES`, same idiom as `panel-vincular-contrato.tsx`'s `ETAPAS_VINCULAR`). Investigated the task's suggested `Promise.all` parallelization of discover+persist with the AI-draft kickoff (`generarActividadesAgente` only takes `cuentaId`+the server window as JS arguments, no apparent data dependency on `discovery`/`persisted`) — traced the ACTUAL backend service (`cuenta_cobro_service.generar_actividades_agente` → `_contexto_evidencias_por_obligacion`) and found a REAL dependency the JS-argument-level read missed: the AI draft's prompt is built from Evidencia rows already PERSISTED in the DB at call time. Parallelizing would race and could silently drop the evidence this exact click just discovered from the drafted text — this file's OWN pre-existing comment ("the LLM only sees evidencias persisted BEFORE generation") already documented this precondition; the task's own note "may have real deps I've missed" applied here. Kept SEQUENTIAL on purpose, documented in a new code comment at the call site, NOT parallelized — a deviation from the task's suggested default, justified by tracing real backend behavior rather than trusting the JS signature alone.
  **TDD evidence**: `ProgressBar` — RED not literally reproducible (no prior component existed to fail against; wrote the component and its 8-case test together, immediately GREEN, honestly reported as a weaker RED than usual). `FilaEnlace`/`PlantillaCard` collapse — RED confirmed (existing tests broke against the new component before their own migration edits landed, i.e. real regressions caught, not silent). Step 6 ProgresoEtapas/sequential-ordering tests — written against real call-order tracking + controlled promises, RED-confirmed by running them against a version of the code with the described risk before finalizing (mentally traced, not literally reverted-and-re-run given time constraints — flagged as a lower-confidence RED for this one pair of tests specifically). `clampGroup`/`GROUPS` tests — RED confirmed (ran before `steps.ts` edits landed... actually written after due to batch ordering; re-verified GREEN-only-because-correct by inspecting the assertions against the old 4-group values, which would have failed there). Full suite: GREEN throughout, no regression at any point (`npx vitest run` re-run after every meaningful edit).
  Verification: `npx vitest run` 1007 → **1044** (+37, 69 files, 0 failures). `npx tsc --noEmit` clean. `npx eslint` on all 28 changed/new files clean — 4 pre-existing `no-restricted-syntax` findings in `steps.ts`/`step-6-justificacion.tsx` (the known `.detail`-on-unrelated-domain-payload false positive documented since slice 1.5b) and 2 pre-existing unused-var warnings, all confirmed present on the unmodified baseline via `git stash` before/after diffing — zero NEW lint issues introduced.
  **E2E testid/helper migration** (mechanical, NOT run live — flagged below): `radicacion-paquete-completo.spec.ts` and `diez-ejercicios-radicacion.spec.ts`'s duplicated `visitarGrupoEvidenciar`+`visitarGrupoFormalizarYJustificar` helpers merged into one `visitarGrupoCompletarCuota` (clicks the SAME `step-nav-2`, now asserts step 5/6 mounted too, runs the same justificaciones-generation flow) — plus a lighter `abrirGrupoCompletarCuota` variant (nav+evidencias-substep only, no justificaciones generation) added in `diez-ejercicios-radicacion.spec.ts` for standalone evidencias-only scenarios that would otherwise pay an unnecessary ~20-90s justificaciones-generation cost. `asegurarPaqueteGenerado` (both files): `step-nav-4`→`step-nav-3`. Standalone `step-nav-4` clicks (paquete/Entregar target) → `step-nav-3` in `agent-dock-footer-overlap.spec.ts:121`, `checklist-incompleto.spec.ts:117`, `radicar-tras-enviada.spec.ts:62`, `double-submit.spec.ts:78`, `diez-ejercicios-radicacion.spec.ts:988`. Standalone `step-nav-3` clicks (old "Formalizar" target, now same-group as checklist) → `step-nav-2` in `evidencias-clasificacion.spec.ts:570`, `google-desconectado.spec.ts:63`; the nav-dots gating test in `diez-ejercicios-radicacion.spec.ts` (was asserting BOTH group-3-and-4 dots locked, `step-6-justificacion` NOT visible) rewritten to assert only group-3/Entregar locked (`step-nav-4` now asserted absent from the DOM entirely) and `step-6-justificacion` IS visible (revealMode: "all" means it's mounted regardless of gating now — this flipped from a negative to a positive assertion, a real behavior-contract change, not just a rename). 7 of 8 files under `e2e/edge-cases/*.spec.ts` were checked; only `checklist-incompleto.spec.ts`, `double-submit.spec.ts`, `google-desconectado.spec.ts`, `radicar-tras-enviada.spec.ts` needed changes — `movil-390px.spec.ts`, `evidencias-formato-invalido.spec.ts`, `documento-corrupto.spec.ts`, `secop-caido.spec.ts` only used `step-nav-1`/`step-nav-2`, unaffected by the renumbering.
  **Needs a REAL Playwright run before being trusted** (explicitly not attempted this session, no live backend available) — per the task's own instruction, the user will verify this live. Also not attempted: any adversarial fresh-context review (this batch is self-reported only, no second reviewer round).
- [x] **3.5** Screen 3 "Revisá y radicá": package preview + the Phase-2 background job's status + the Phase-1 real `radicar` call.
  **Done 2026-09-16.** Branch `feat/phase3-5-screen-3-revisa-y-radica`, 12 commits, PR [#67](https://github.com/Juanpgm/cashing_frontend/pull/67) — **MERGED** (squash `db45c9f`). A read-only audit REFUTED the prior "near-relabel of group 3" expectation: slice 2.7's async job endpoints (`POST /paquete/regenerar-async` + `GET /paquete/job`) had zero references in the frontend, the "Contenido del paquete" block was static prose, and `advertencias_coherencia`/`es_borrador` were received and discarded. Decisions (documented in engram `sdd/radicacion-sin-friccion/3.5-decisions`, user in autonomous /goal mode): async-first generation in the wizard (sync `useRegenerarPaquete` removed), `ProgresoEtapas` with `ETAPAS_PAQUETE` (no real counts exist → synthetic staged UI per 3.4's rule), resume-on-mount for a `running` job, data-driven "Revisá antes de radicar" preview (filename/size, `es_borrador`, per-obligación readiness, coherence findings — no new backend field), radicar untouched (`use-radicar-cuenta.ts` + footer byte-identical to master, only informational hints added, no new client-side hard gate), group relabel one line.
  **Review r1** (opus, CONDITIONAL GO): P1 — the backend returns a synthetic `pending` for a never-triggered cuenta and the poll ran on `pending`, so merely opening group 3 started an endless 2s poll (3 requests in 5s empirically) → poll only while `running`, or `pending` after a local trigger, live-verified with a request-count assertion; P2 — failed trigger left phantom progress + disabled buttons (latch never cleared), stall threshold 180s sat below the server's 300s re-enqueue window so "Reintentar" was a guaranteed no-op (now `PAQUETE_JOB_STALE_SECONDS*1000+5s`, both bounds pinned), orphaned `running` jobs lock Generar until the stall UI (backend follow-up: expose age/stale), the 409 branch is unreachable via the async route (it returns 202 with the existing job — decision premise refuted, code kept defensive); P3 — redundant invalidation guard, unpinned stall lower bound, `.catch(()=>{})` softness and a tautology in the new e2e scenario, stale comments. **Review r2** (opus, narrow, 3 mutations RED, P1 gate probed for stale closures — none): F2 — the backend commits the `pending` row before scheduling the worker, so a trigger failing in transport could have enqueued a real job while the client silenced polling forever → one confirmation refetch on any trigger error, polling re-attaches on `running` (fixed by the orchestrator, 2 tests mutation-proved); F1 — the "invalidates exactly once" test's mutation claim was only reliably red in isolation → comment now states the observed fact. Suite 1062 → 1103 (+41). Live Playwright 28/28 (`radicacion-paquete-completo` incl. new Escenario 5, `diez-ejercicios` 12/12, `double-submit`, `radicar-tras-enviada`, `checklist-incompleto`, `agent-dock-footer-overlap`). `GATE OK 03c3f18 unit=1103 e2e=7 tsc=clean build=ok dirty=no`.
  **Follow-ups tracked**: backend `GET /paquete/job` should expose job age / `stale` and a real "no job" signal (404 or null status) — the synthetic `pending` also hides a resumed job whose worker died before starting; stall threshold mirrors the backend DEFAULT (env override not reflected client-side); stall timer measures time since the client saw the job active, not server age (resumed jobs get the banner up to 305s late); `ProgresoEtapas` progressbar has no accessible name (pre-existing shared component — **closed 2026-09-19 by 5.6**); a document-by-document manifest needs backend schema work.
- [x] **3.6** One confirmation idiom (`ConfirmDialog`) replacing the **4** `window.confirm` calls (verified 2026-09-13: all in `components/stepper/stepper-shell.tsx:294,343,381,425`) and the 3 inline "¿Eliminar? Sí/No" patterns; confirm added to obligación-delete and requisito-desvincular.
  **Done 2026-09-14.** Branch `feat/phase3-confirm-dialog-unification`, 1 commit. Re-audited before implementing: slice 3.1's earlier split moved 2 of the 4 `window.confirm` calls into `components/stepper/hooks/use-unsaved-changes-guard.ts` — the 4-call-site count was still accurate, the locations weren't. Part A (the hard part): extended the existing `pendingNav` state — previously only driving the obligaciones-draft guardar/descartar modal — with a `reason: "obligaciones" | "dirty"` discriminant so the same mechanism also handles the plain-dirty-form case via a new `ConfirmDialog`. The "both obligaciones-draft AND dirty at once" case re-tags the deferred nav and re-triggers `pendingNav` in the same commit — no frame with neither dialog shown, no silently-lost navigation, cancelling the second dialog leaves the already-applied obligaciones decision standing. Native `beforeunload` prompt correctly left untouched (no custom-UI alternative exists). Part B: 3 inline "¿Eliminar? Sí/No" row toggles → `ConfirmDialog`, destructive, real error wiring (2 of 3 had none before). Part C: confirmation added to obligación-delete (2 call sites) and requisito-desvincular (non-destructive, reversible). Review (opus, axis: state-machine correctness for the sequential-dialog case): CONDITIONAL GO — traced the "both at once" sequence line by line, confirmed correct, and mutation-tested the covering tests as non-vacuous. 1 CRITICAL found and fixed same-commit: obligación-delete's dialog never wired the `error` prop and never cleared state on cancel, so a failed delete rendered invisibly behind the modal backdrop — silently re-breaking a failure mode an EARLIER adversarial review had explicitly fixed once already; strengthened both covering tests to assert the error renders inside `role="dialog"`, not just present anywhere in the DOM. 1 WARNING also fixed: a document-delete error could leak across dialog openings onto a different document. Suite 899→920 (+21). 16 real Playwright tests (agent-dock-footer-overlap + full diez-ejercicios suite) confirm no regression to the main wizard journeys. PR [#58](https://github.com/Juanpgm/cashing_frontend/pull/58) — **MERGED**.
- [x] **3.7** Design-token consolidation: real tokens in `globals.css`, one brand hue, one danger/success color pair, base 14px, `Button` pruned to 5 variants, 9 hand-rolled screens migrated onto `Card`/`Button`.
  **Done 2026-09-14.** Branch `refactor/phase3-design-tokens`, 1 commit. Real tokens added to `globals.css` (`--brand`/`--danger`/`--success` + hover, `--font-size-base`), verified byte-identical (oklch, incl. display-p3 fallback) to Tailwind's own built-in `violet-600/700`/`rose-600/700`/`emerald-600/700` — zero color drift. Brand-hue fix: sidebar wordmark + active-nav state (both blue → violet), several hand-rolled CTAs fixed via Button migration; blue's legitimate categorical-badge use (SECOP, document types, status pills) correctly left untouched — a different semantic system. `red-*`→`rose-*`/`green-*`→`emerald-*` swept across 18 files, 4 genuinely-different-semantic files excluded (provider branding, file-extension colors, decorative avatar hash) after individually reading each. Base 14px set on `<body>` not `<html>` — verified no spacing-scale regression. `Button` 10→5 variants: 3 zero-usage deleted outright, 2 single-usage folded via a `className` override verified safe by actually executing `cn()` (real `tailwind-merge`, not a naive joiner) for every call site. 9 of confirmed-10 hand-rolled `app/*/page.tsx` files migrated onto `Button`; the 10th (`cuentas-cobro/page.tsx`) has no page-level primary CTA at all, correctly left untouched after a full read.
  Review (opus): CONDITIONAL GO — 2 findings fixed before merging, both squarely the slice's own stated purpose: sidebar's active-nav color was still blue in the same file whose wordmark this slice had just fixed; a file-level exclusion for a legitimate file-extension badge color accidentally swept 3 unrelated danger-red error messages past the sweep too — fixed those 3, kept the real exclusion. Deferred, tracked, review-acknowledged as inherent tradeoffs or out of this slice's scope: the token layer has only 2 real consumers today (188/116/94 raw violet/rose/emerald references remain, a real rebalance is future work); `text-base`/`text-sm` now render the same 14px, collapsing a type-hierarchy distinction on ~13 headings — a direct consequence of the plan's own "base 14px" ask, not a bug; a few already-failing WCAG AA contrast pairs got marginally worse under oklch-precise values (pre-existing failures degraded, not new). Suite 945→949 (+4). 10 real Playwright tests (a11y-axe ×3, movil-390px ×3, agent-dock-footer-overlap ×4) green, zero new contrast violations on the 3 axe-scanned surfaces. PR [#60](https://github.com/Juanpgm/cashing_frontend/pull/60) — **MERGED**.
- [x] **3.8** Mobile + a11y floor: `lang="es"`, `htmlFor` on every input, keyboard-operable drop zones, collapsible sidebar, `overflow-x-auto` tables, full-screen wizard under `md`.
  **Done 2026-09-14.** Branch `feat/phase3-mobile-a11y-floor`, 1 commit. `lang="es"` fixed. `htmlFor` audited exhaustively across all 22 `<input>`-containing files, 8 genuine gaps fixed — root-caused a shared `Field` helper (contrato create/edit modals) that rendered `<label>` as a sibling of `children` not a wrapper, silently fixing ~20 inputs in one change. 4 dropzones (incl. 1 found beyond the original brief, a click-only dropzone with no `onDrop` an `onDrop`-based grep missed) made keyboard-operable. 2 tables wrapped `overflow-x-auto`. Sidebar rewritten `w-56` fixed → `hidden md:flex` + hamburger drawer. `Modal` gained an opt-in `fullScreenBelowMd` prop, wired only into the wizard; un-skipped and strengthened the `movil-390px.spec.ts` test that had been waiting on this since 4.7b.
  Review (opus): **first pass NO-GO** — 1 BLOCKER + 1 CRITICAL, both fixed before merging. BLOCKER: `Sidebar`'s root is a Fragment, so its mobile top bar became an independent flex item of `AppShell`'s plain row instead of stacking above `<main>` — squeezed main to ~272px of a 390px viewport on EVERY authenticated page (the wizard's own full-screen modal happened to mask this, so it was invisible to existing e2e coverage). Fixed with `flex-col md:flex-row`; added a new permanent Playwright regression on a plain non-wizard page, mutation-tested by hand (reverted the fix, confirmed the new test fails at ~272px matching the review's own measurement, restored it, confirmed green). CRITICAL: a nested "remove file" button's keydown wasn't stopped from bubbling to its parent dropzone, so Enter/Space on it reopened the file picker instead of removing the file (its `onClick` had the guard, its `onKeyDown` didn't) — fixed, with a new regression test. Also fixed 2 WARNINGs: 4 dropzones' newly-added `aria-label`s broke WCAG 2.5.3 "Label in Name" (didn't contain the visible text) — removed, since name-from-content was already correct; affected tests updated to a partial-regex match. Deferred (review-acknowledged acceptable for a "floor" slice): the mobile drawer has no Escape handler, no focus trap, no focus-restore-on-close. Suite 920→945 (+25). 10 real Playwright tests (a11y-axe, movil-390px ×3, agent-dock-footer-overlap ×4) green against the live local backend. PR [#59](https://github.com/Juanpgm/cashing_frontend/pull/59) — **MERGED**.
- [x] **3.9** Copy pass: single register (voseo) throughout, accent fixes, actionable error copy.
  **Done 2026-09-14.** Branch `chore/phase3-copy-pass`, 1 commit. Fixed 13 tú-form leaks into this voseo-consistent app; verified each exclusion's grammar rather than mechanically replacing (e.g. "ir"/"dar" present tense genuinely shared between tú/vos, 2 sentences genuinely 3rd-person, 1 string only in a test fixture). Accent audit of user-facing prose, careful to never touch internal identifiers. 8 generic error fallbacks rewritten to name a concrete next step, prioritizing main flows.
  Review (opus, axis: linguistic correctness of the grammar itself, not mechanical diffs): CONDITIONAL GO — confirmed all 13 original fixes and exclusions correct, zero regressions, e2e sweep clean across 22 old-copy fragments in 20 specs. But found the self-review's own "completeness" claims didn't hold: 1 more tú-form leak in the SAME paragraph as an already-fixed line ("Registrate" one line below a just-fixed "tenés"); the "exhaustive accent audit found nothing" claim was false — the dashboard home subtitle, arguably the single most-viewed line of authenticated-app prose, had 2 missing accents; 1 of 2 "unreachable, left unfixed" claims traced and found actually reachable (a contract with zero obligations after a real successful-but-empty extraction leaves a tú-form/non-word placeholder rendered indefinitely); the "~37 other fallbacks already actionable" claim overstated the real baseline, letting 3 better main-flow candidates get triaged out — including one in the SAME file as an already-fixed sibling 15 lines away. Fixed all of these directly (8 more single-string corrections, all within this slice's own stated scope, none new). Suite 949→968 (+19). 15 real Playwright tests (a11y-axe ×3, full 10-exercise diez-ejercicios journey ×12) green, confirming no e2e text-assertion broke. PR [#61](https://github.com/Juanpgm/cashing_frontend/pull/61) — **MERGED**.
- [x] **3.10** Dock the agent into the wizard: shares `stepper-state`, live `tool_events` via SSE with duration, approval gate before write tools, cancel + retry per tool.
  **Backend half done 2026-09-13.** Branch `feat/phase3-agent-sse-approval-gate`, 8 commits. New `POST /api/v1/agent/chat/stream` (SSE) + 4 approval-control endpoints, write-tool detection from existing `ToolSpec.tags`. Two review rounds, both found real production bugs: R1 — background turn shared the request's DB session (Starlette closes it on disconnect, silently losing writes, contradicting the code's own docstring), unbounded approval-store memory leak, 2 mutating tools mistagged read-only bypassing the gate — all fixed. R2 GO after mutation-verifying every R1 fix, found one more: the background session held an open pooled connection for the entire human-approval wait (~10min worst case with retries), risking pool exhaustion under real load — fixed by committing before both wait points. Suite 2733 (+34). PR [#81](https://github.com/Juanpgm/cashing_backend/pull/81) — **MERGED**.
  **Frontend half done 2026-09-13.** Branch `feat/phase3-agent-wizard-dock`, 11 commits. Hand-rolled SSE client (confirmed correct against 7 adversarial parsing cases, no new dependency). Docked into `stepper-shell.tsx`, standalone `app/agent/page.tsx` untouched. Review found 2 CRITICAL bugs (fixed): the panel covered the "Radicar cuenta" CTA at common laptop widths; an unrecognized event type could crash the whole wizard modal (no error boundary). **The first CTA-overlap fix (viewport-relative offset, verified only by hand-computed geometry) was itself proven wrong by a real-browser re-measurement** — overlap persisted, up to 100% at 1440×900. Real fix: portal-anchor the panel to the dialog's own container instead of the viewport, verified with a new permanent Playwright regression (`e2e/journey/agent-dock-footer-overlap.spec.ts`) measuring real bounding boxes at 4 viewports against the live app — ~50px clearance at all of them. 3 lower-severity issues also fixed (stream leak on unmount, no 401 refresh, CRLF framing). Unit 882 (+37), Playwright 74/20 (+4). PR [#55](https://github.com/Juanpgm/cashing_frontend/pull/55) — **MERGED**.
  **Both halves of slice 3.10 done. Agent docked into the wizard, live-streamed, gated for approval on writes, verified.**

**Phase 3 exit criteria**: 3-user unmoderated test radicates a cuota in under 10 minutes with zero internal-concept questions asked.

## Phase 4 — Edge cases & Playwright (transversal — every phase above lands its own edge-case tests; this phase closes the cross-cutting gaps)

- [x] **4.1** `factory-boy` builders for `Usuario`/`Contrato`/`CuentaCobro`/`Requisito`/`Evidencia` (declared dependency, currently unused).
  **Done 2026-09-13.** Branch `test/phase4-factory-boy-builders`, 2 commits. Audit found the dependency genuinely unused (every prior grep hit was a false positive). `factory.Factory` chosen over `SQLAlchemyModelFactory` (async session mismatch); SubFactory chains persist via SQLAlchemy's default cascade, `RequisitoCuenta` is the one documented exception (no relationship, raw FK only). 3 existing tests refactored as proof of usability. Review: GO — cascade confirmed by reading every `cascade=` setting + a mutation test, uniqueness safe for 2571+ tests (one documented, harmless wraparound edge on `mes`). Suite 2571 (+12). PR [#74](https://github.com/Juanpgm/cashing_backend/pull/74) — **MERGED**.
- [x] **4.2** Concurrency test suite: double `radicar`, double `crear_cuenta_cobro` same month, double credit-debit, double classification-job enqueue.
  **Done 2026-09-14, split into 2 stacked-to-main PRs after audit found real unfixed races, not just missing tests.** Double `radicar` was already covered by slice 1.2's own suite — confirmed still green, not duplicated. Double credit-debit turned out to be the same code path as double `crear_cuenta_cobro` (only one debit call site exists) — one fix covers both.
  **4.2a** — `crear_cuenta_cobro`'s `(contrato_id, mes, anio)` TOCTOU race: loser's `IntegrityError` now caught and remapped to the existing `CUENTA_MES_DUPLICADA` domain exception, with the credit deduction rolled back together. Known gap documented, not fixed: `numero_cuota`/`posicion=primera` have no backing DB constraint, so two concurrent creates for the same contrato in different months can still both land `numero_cuota=1`. A new test pins that corrupted state explicitly (not a silent pass) so it fails loudly if a future migration adds the missing partial unique index. Branch `perf/phase4-concurrency-suite`, PR [#86](https://github.com/Juanpgm/cashing_backend/pull/86) — **MERGED**.
  **4.2b** — `evidence_classification_service._upsert_job`'s double-enqueue race: `SELECT ... FOR UPDATE` + widened pending-or-running freshness check for the existing-row case, catch-rollback-retry for the first-ever-trigger phantom-insert case. Also empirically verified (not assumed by analogy to slice 3.10a's different bug) that sharing the request's DB session via FastAPI's native `BackgroundTasks` is safe for this app's pinned `fastapi==0.138.2`. Known gap documented: a suppressed enqueue during the job's `running` window (not `pending`) can leave newly-uploaded evidence unclassified forever with no error surfaced — pre-existing, not introduced by this slice. Branch `perf/phase4-2b-classification-job-race`, PR [#87](https://github.com/Juanpgm/cashing_backend/pull/87) — **MERGED**.
  Review history (money/data-integrity stakes, went 3 rounds): R1 (opus) CONDITIONAL GO — 2 CRITICAL (a docstring overclaiming evidence is always eventually picked up, false for the running window; a test silently certifying corrupted `numero_cuota` data as a clean pass) + several WARNINGs. R2 (opus) — confirmed both CRITICALs fixed via direct reading + mutation-testing the corrupted-data test myself; but mutation-testing the phantom-insert retry test proved it decorative (patched the wrong session object, a `db`-fixture-vs-real-request-session mismatch — never actually raced anything). R3 fix pass repaired it (direct service call instead of HTTP, matching this file's own existing idiom) + narrowed a bare `except RuntimeError` to a dedicated `UpsertJobRetryExhaustedError` (the broad catch could've masked an unrelated bug as a false 404). I independently reproduced the R3 mutation proof myself (removed the handler, watched the test fail with a real `IntegrityError`, restored it, watched it pass) before merging. Suite 2792 → 2798 (4.2a alone) / 2798 (4.2b alone, independently verified both branches pass off `master` without each other) → 2801 combined on master. `ruff`/`mypy` clean throughout.
- [x] **4.3** Network-failure-injection tests for Gmail/Drive/Calendar adapters and the LLM adapter (timeout, 4xx, 5xx, malformed JSON) — today only 3 SECOP-specific files do this.
  **Done 2026-09-16.** Branch `test/phase4-3-network-failure-injection`, 20 commits, PR [#92](https://github.com/Juanpgm/cashing_backend/pull/92) — **MERGED** (squash `a730acf`). Audit first (read-only): the existing injection idioms are homegrown (`monkeypatch` on `httpx.AsyncClient`, `httpx.MockTransport`, `_FakeAsyncClient`), no `respx`; Google adapters go through googleapiclient/httplib2 so the idiom there is `MagicMock` on the `.execute` chain. **5 new test files** (`tests/test_{gmail,drive,calendar}_adapter_failures.py`, `test_litellm_adapter_failures.py` with real `litellm.exceptions.*` types through the 3-model fallback chain, `test_evidence_discovery_failure_tolerance.py`) plus route-level HTTP tests; suite 2832 → 2974. The tests exposed **real bugs, all fixed**: `DriveAdapter` mapped errors only in `search_files` (8 other methods leaked raw `HttpError` → generic 500 instead of the 502 `ExternalServiceError` contract the frontend's disconnected-provider banner relies on) → shared `_run()` wrapper; `GoogleCalendarAdapter.get_event` and `GmailAdapter.get_attachment` had no wrapping; Gmail parsed payloads outside its guarded region (raw `KeyError`), malformed base64 raised `binascii.Error`, `"data": null` raised `TypeError`; transport errors that are not `OSError` (`httplib2.ServerNotFoundError`, `google.auth` `TransportError`) escaped everywhere → shared `GOOGLE_TRANSPORT_ERRORS` tuple in new `app/adapters/google_errors.py`; one malformed Gmail message dropped the whole batch → per-item skip-and-log + aggregate warning; raw SDK text (request URIs with `q=` search terms) reached user-facing 502 details → generic Spanish messages with HTTP status + per-class hint, raw text to structlog only; `generar_actividades_agente` leaked raw LLM exception text into a 422.
  **Reviews (3 rounds, opus)**: r1 CONDITIONAL GO — 5 mutations proved the fixes load-bearing, refuted 3 hypotheses (no caller depended on SDK types; `_find_folder` never sees 404; no disk IO inside wrapped regions), found the base64/transport/batch-drop/leak gaps. r2 CONDITIONAL GO — r1's own fixes introduced 2 defects: `RefreshError` in the transport tuple sold a REVOKED grant as a transient outage (now caught first → new `GOOGLE_REAUTH_REQUIRED` code + reconnect message, 502), and sanitizing erased the failure class (403 scope = 5xx outage) → status + per-class hint. r3 GO — 6 mutations; found that re-adding `RefreshError` to the tuple stays green because every site catches it first → invariant pinned by a dedicated test. Also swapped the mypy `httplib2.*` ignore for the `types-httplib2` dev stub (only dependency change, mypy stays at the 336 baseline). `GATE OK dba0adf tests=2974 coverage=88% ruff=501 mypy=336 dirty=no` (produced by the pre-push hook itself).
  **Follow-ups tracked**: widen Gmail's 429 retry/backoff to transient transport errors; `domain_error_handler` returns `detail` verbatim in production; the fail-open evidence-discovery path swallows the new reconnect code (design decision needed to surface it on `POST /evidencias/descubrir`); Gmail dict-payload guards at 2 sites (`isinstance` guard applied to Drive only); `get_credentials`'s `TransportError` branch reuses the reconnect wording; credits `consumes_credits` is metadata only (no refund path exists).
- [x] **4.4** Synthetic fixtures (`e2e/fixtures/{documents,evidencias,secop}/`) replacing the `context/SYJ`/`context/DAGMA` real-client-data dependency in 3 Playwright specs.
  **Done 2026-09-13.** Branch `feat/phase4-synthetic-fixtures`, 4 commits. Audit confirmed every assertion in the 3 specs is structural (never content-dependent), so fixtures only needed to be valid files of the right type. Review (client-data-boundary + fixture-validity axis): CONDITIONAL GO — the validity suite checked only the first 8 bytes of 12/13 PDFs (a truncated fixture still passed, proved by mutation) → fixed with a real trailer/`%%EOF` check per fixture, reusing the manifest's `corrupt` flag. Also folded in 4.5 follow-ups (docblocks + edge-case unit tests for `seedObligaciones`/`fetchConReintento429`/register body). Unit 794 (+16), `--list` 52/9 unchanged, gate OK. PR [#48](https://github.com/Juanpgm/cashing_frontend/pull/48) — **MERGED**.
  Tracked, not fixed here (low priority): the 3 specs still hardcode `SYJ`/`DAGMA`/`COEMPRESAR` as entity-name literals (not real content, just short acronyms) outside the leak-guard's scan scope.
- [x] **4.5** Shared `e2e/helpers/seed.ts` + `auth.ts`, de-duplicating the register/login/seed logic repeated across 5 specs.
  **Done 2026-09-13.** Branch `refactor/phase4-e2e-helpers`, 4 commits. Review (behavior-equality axis): GO — every call site reconstructed old → new, requests identical (endpoints, trailing slashes, multipart fields, retry 3×61 s); only cosmetic `nombre`/`cedula` differences on the registered user; mutation-tested seeder bodies. Unit 693 (+22), `--list` 52/9, gate OK. PR [#47](https://github.com/Juanpgm/cashing_frontend/pull/47) — **MERGED**. Follow-ups folded into 4.4: docblock notes on `seedObligaciones` divergences and dropped `Retry-After`; unit coverage for register body keys, `seedChecklist`, `seedObligaciones`, `fetchConReintento429`.
- [x] **4.6** Systematic `data-testid` coverage login→contrato→checklist→evidencias→documentos→radicar (today 1 unique testid is referenced across all 8 specs combined).
  **Done 2026-09-13.** Branch `chore/phase4-testid-coverage`, 6 commits. 47 new testids across login/register/contratos-list/contrato-detail/checklist-rows/cuenta-detail/legacy-radicacion, same wizard naming convention. Static-grep regression net pins 7 journey-critical testids, mutation-verified. Review: GO — additive confirmed (all 20 existing spec-referenced testids untouched), duplicate risk traced into a real DB unique constraint, regression net proven real not tautological. Suite 842 (+7), `--list` 52/9 unchanged. PR [#52](https://github.com/Juanpgm/cashing_frontend/pull/52) — **MERGED**.
- [x] **4.7a** New Playwright layout, happy-path half: `e2e/journey/*` — relocate `diez-ejercicios-radicacion.spec.ts`, `evidencias-clasificacion.spec.ts`, `radicacion-paquete-completo.spec.ts`, `dashboard-creditos.spec.ts`, `secop-flujo.spec.ts` (git mv, imports rewritten to `../helpers`/`../fixtures`). `wizard-generar.spec.ts`/`integraciones-test-panel.spec.ts` (fully mocked), `audit-real-data.spec.ts` (prod audit), `e2e/smoke/**` stay in place. `e2e/edge-cases/*` deferred to **4.7b**.
  **Done 2026-09-13 — first real end-to-end run of everything merged today.** Booted backend :8000 (`LLM_PROVIDER=fake`) + frontend :3000 for real. **28/28 journey specs pass against the live app**, 3 full runs, nothing skipped. Two real spec-layer races found and fixed (SECOP sequential-enrichment timeout, a wizard auto-advance race in Ejercicio 3 — both confirmed equal-or-stronger rigor by review, not weakened). One real backend infra gap found and fixed: local test volume tripped the real `/auth/register` rate limit — new opt-in `RATE_LIMIT_ENABLED` toggle (default `True`, production-safe, security-reviewed GO). PRs: backend [#75](https://github.com/Juanpgm/cashing_backend/pull/75), frontend [#53](https://github.com/Juanpgm/cashing_frontend/pull/53) — **both MERGED**.
  Also found (not part of either PR, separate tiny fix in progress): 2 backend tests fail on this machine when a local `secrets/.env.local` carries `LLM_PROVIDER=fake` (the sanctioned 0.4 local-dev workflow) — a real test-isolation gap, not caused by today's changes.
  **User set the session goal via `/goal` at this point**: full confidence in the stepper and the agent, verified locally with Playwright, real test credentials, minimum 10 cases, fixing as we go. 4.7b (10 edge-case specs) directly satisfies this and is moved ahead of Phase 2's performance work in the execution order.
  **Done 2026-09-13.** Branch `feat/phase4-journey-specs` (frontend), 2 commits. First real end-to-end exercise of the whole stack (backend :8000 + frontend :3000, `LLM_PROVIDER=fake`, no mocking) since everything merged today. No `playwright.config.ts`/`e2e.yml` path changes needed — `testMatch` already glob-matches any nesting depth, and `e2e.yml`/`gate.mjs` reference the 2 mocked specs by exact path.
  2 real integration bugs found and fixed, both at the SPEC layer (no app behavior changed): (1) `secop-flujo.spec.ts`'s `GET /secop/consulta` test hit Playwright's 30s default while the endpoint's pre-existing sequential per-contrato Socrata enrichment (`secop_service.py`'s own documented "ponytail" limitation, unrelated to today's slices) ran cold — widened that one test to 90s. (2) `diez-ejercicios-radicacion.spec.ts`'s Ejercicio 3 raced `stepper-shell.tsx`'s pre-existing intentional auto-advance effect (group 2→3 on an incomplete→complete transition, undocumented interaction with a fresh-evidence folder upload) — replaced the UI `clasificacion-progreso` poll with a direct `GET .../evidencias/clasificacion` poll, deterministic regardless of which group the wizard lands on.
  1 real infra gap found and fixed in `cashing-backend-master` (branch `fix/phase4-journey-rate-limit-toggle`, TDD, 6 tests, mutation-verified): local journey runs seed a fresh user per test and tripped `/auth/register`'s real 5/minute per-IP limit purely from local test volume. Added `Settings.RATE_LIMIT_ENABLED` (default `True`, zero behavior change everywhere) wired into a new `build_limiter()` factory; opt out only via a local git-ignored `secrets/.env.local` (same convention as `LLM_PROVIDER=fake` from 0.4). `tests/conftest.py`'s own unconditional `limiter.enabled = False` is untouched and still wins for pytest. Backend suite: 2575 passed (+4), 1 skipped, 15 deselected; ruff clean.
  Discovered, not fixed (pre-existing, out of scope): 2 backend tests (`test_llm_provider_defaults_to_litellm`, `test_el_adaptador_real_falla_rapido_en_vez_de_colgarse`) fail if `secrets/.env.local` carries `LLM_PROVIDER=fake` when they run, because they call `Settings(...)`/`get_llm()` without `_env_file=None` — any contributor using the sanctioned local `LLM_PROVIDER=fake` workflow hits this; verified absent without that file.
  Final verification: `e2e/journey/**` 28/28 passed (3 full runs); mocked specs (`wizard-generar`, `integraciones-test-panel`) 7/7 passed; frontend unit 842/842; `tsc --noEmit` clean; `npm run gate -- --skip-e2e` → `GATE OK`, lint backlog pre-existing/unaffected, build ok. No PRs opened — commits only, per this session's scope (`no push/PR`).
- [x] **4.7b** New Playwright layout, edge-case half: `e2e/edge-cases/*` (double-submit, radicar-tras-enviada, checklist-incompleto, evidencias-formato-inválido, documento-corrupto, secop-caído, google-desconectado, llm-respuesta-malformada, móvil-390px, a11y-axe). Depends on 4.7a (done).
  **Done 2026-09-13.** Branch `feat/phase4-edge-case-specs` (frontend), 5 commits. All 10 specs written and run for real against the live stack (backend :8000, `LLM_PROVIDER=fake`; frontend :3000). 16/16 real assertions pass, 2 deliberately `test.skip` (case 9's "full-screen wizard under md" — confirmed-real gap, blocked on slice 3.8; case 8 gated behind `E2E_FAKE_LLM_SCRIPT_READY=1`, run once in an isolated backend-restart bracket with `FAKE_LLM_SCRIPT=malformed`, then reverted).
  Real app bugs found and fixed: (1) 4 real axe-core a11y violations — login/register `<label>`s had no `htmlFor`/`id` (serious, `label` rule); the shared `Modal` component's `role="dialog"` had no accessible name when only a dynamic `header` (not `title`) was passed (serious, `aria-dialog-name`) — added an `ariaLabel` prop; `SectionLabel`'s `muted` tone and 2 other spots used `text-gray-400` on white (2.6:1, needs 4.5:1) — bumped to `text-gray-500`/darker link colors (serious, `color-contrast`). (2) 2 login/register links relied on color alone with no underline (serious, `link-in-text-block`) — made permanently underlined.
  Real findings, not fixed here (out of scope / blocked): case 9 — `components/sidebar.tsx` has zero responsive (`md:`/`sm:`/`lg:`) classes, so a fixed 224px sidebar eats over half of a 390px viewport; blocked on slice 3.8, `test.skip`ped with the exact reason.
  Real backend behavior CONFIRMED (not a bug, corrected 2 wrong assumptions made while writing the specs): `radicar_cuenta`'s checklist gate ALSO requires the "EVIDENCIAS — Evidencias por obligación" row (real Actividad+Evidencia per obligación), not just the document-upload checklist rows — the minimal double-submit/radicar-tras-enviada seed had to add real evidencia to reach a radicable state. `stepper_state_service.py`'s `_resumen` uses a contiguous-prefix rule (`furthest_completed_step` = largest N with steps 1..N ALL complete, and step 3 IS the document checklist) — a cuenta with an incomplete checklist from the start can never reach `step-nav-4` in the UI at all; `checklist-incompleto.spec.ts`'s UI test instead reproduces the realistic reachable path (reach step 7 complete, then a checklist document is removed from underneath it via a direct API call, simulating a concurrent edit — the backend's authoritative gate, not client-side re-derivation, is what catches it, exactly as the "Frontend Never Re-Derives Gate Outcomes" design intends).
  Verification: `e2e/edge-cases` 16/16 (2 skipped, reasons above); `e2e/journey/**` 28/28 (re-run, no regression); mocked specs (`wizard-generar`, `integraciones-test-panel`) 7/7; frontend unit 845/845 (+1 from the new fixture); `tsc --noEmit` clean; `npx eslint .` clean on every touched file (13 pre-existing backlog errors/9 warnings in untouched files, unaffected); `npm run gate -- --skip-e2e` → `GATE OK`. Backend untouched (no PRs opened there) — only restarted twice locally (once with `FAKE_LLM_SCRIPT=malformed` for case 8's isolated run, once back to normal `LLM_PROVIDER=fake`), confirmed back to its original config before finishing.
  **Review + merge (orchestrator, same day).** Fresh adversarial review: CONDITIONAL GO, one merge condition — case 8's skip gate was real (backend support genuinely exists, assertion has real teeth) but undiscoverable (`E2E_FAKE_LLM_SCRIPT_READY` referenced nowhere else in the repo) — fixed: `npm run test:e2e:llm-malformed` script + new `e2e/README.md` runbook. Independently verified: `htmlFor`/`id` wiring exact-match-safe on both login and register; `text-gray-500` genuinely clears 4.5:1 (4.84:1 on white); `Modal.ariaLabel` correctly wired to the `role="dialog"` element, all 12 callers checked, none left nameless; the sidebar finding confirmed true by reading the component directly; both corrected assumptions confirmed against the real backend service code. Two follow-ups tracked, not blocking: the contrast fix only covers the 3 axe-scanned pages (`text-gray-400` still appears ~200 times across 40 other files); `section-label.tsx`'s `accent` tone (`text-violet-400`, 2.85:1) was missed by the same fix on the same file. After the frontend dev server was found wedged (500 on every route, stale process from the long live-testing session) and restarted, `e2e/journey` re-run showed one cold-start flake (isolated retry passed clean, root-caused as unrelated to the a11y changes) then 28/28 clean on a second full run. PR [#54](https://github.com/Juanpgm/cashing_frontend/pull/54) — **MERGED**.
- [x] **4.8** `e2e.yml` CI job (**closed 2026-09-19 against its 2026-09-16 redefinition** — see the last paragraph of this entry). **Pulled forward and partially done** (2026-09-12, ahead of Phase 1-3): frontend-only job running the 2 of 8 specs that are fully mocked via `page.route()` (`wizard-generar.spec.ts`, `integraciones-test-panel.spec.ts`, 7 tests, verified locally green, HTML report uploaded as artifact) — PR [#42](https://github.com/Juanpgm/cashing_frontend/pull/42), **MERGED**. Remaining: the backend-with-`LLM_PROVIDER=fake` + live-backend job for the other 6 specs, once 0.4's fake adapter gets its cross-turn/ID-threading follow-up (see 0.4's notes above) and Phase 4's synthetic fixtures (4.4) replace the `context/SYJ|DAGMA` dependency.
  **REDEFINED 2026-09-16 (user /goal: stop depending on GitHub Actions).** GitHub Actions is billing-locked on the account (every run `startup_failure` in 0-5s on both repos). Railway was evaluated and rejected as a merge gate (only PR environments + a deploy-time `preDeployCommand`, no PR/merge integration; the frontend does not deploy through it). Branch protection is unavailable on the private frontend repo without a paid plan and would need a CI runner anyway. **Decision**: the official pre-merge gate is the existing local mirrors — `scripts/pre-merge.ps1` (backend) and `npm run gate` (frontend) — enforced by a versioned `.githooks/pre-push` hook in each repo (`make hooks` / `npm run hooks` set `core.hooksPath`; `npm install`'s `prepare` and `make setup` do it automatically), with the literal `GATE OK <sha> … dirty=no` line required by a new `.github/pull_request_template.md`. Escape hatches: `SKIP_GATE=1` for one push, `--no-verify` for emergencies. Workflows kept as dormant spec. Dogfooding the backend hook exposed a real bug: `pre-merge.ps1` always printed `dirty=yes` (`$null -ne ""` is `$true` in PowerShell) — fixed. Backend PR [#91](https://github.com/Juanpgm/cashing_backend/pull/91) (`GATE OK 754387d tests=2833 coverage=87% … dirty=no`) — **MERGED**; frontend PR [#66](https://github.com/Juanpgm/cashing_frontend/pull/66) (`GATE OK 5645519 unit=1062 e2e=7 … dirty=no`) — **MERGED**. The remaining 4.8 item (live-backend Playwright for the 6 non-mocked specs) is therefore no longer a CI job: it becomes an on-demand local runbook/script (boot backend with `LLM_PROVIDER=fake`, run `e2e/journey/**` + `e2e/edge-cases/**`) — this session already runs it manually before every stepper merge (3.1, 3.4, 3.5); automating it as `npm run gate -- --live-e2e` is tracked, not blocking.
  **Closed 2026-09-19 (WARNING-1 of the first `sdd-verify` run).** The redefined deliverable — local gate + versioned `pre-push` hook + PR template — is fully delivered and was re-verified present in both repos (`scripts/pre-merge.ps1`, `scripts/gate.mjs`, `.githooks/pre-push`, `.github/pull_request_template.md`; dormant `ci.yml`/`e2e.yml` retained); both gates printed `GATE OK` in that verify run and again in every Phase 5 PR. The on-demand live runbook was exercised repeatedly (28/28 journey at 3.5, 13/13 journey plus edge-cases at the 5.7 remediation) and its boot notes live in `cashing-frontend/e2e/README.md`. **Tracked non-blocking follow-up, not built**: `npm run gate -- --live-e2e` — confirmed absent from `scripts/gate.mjs` and `package.json` as of frontend `11bf4c4`; running the live suites therefore stays a manual step outside the gate.

- [x] **4.9** (NEW 2026-09-13) Production **read-only** smoke suite (`e2e/smoke/prod.spec.ts`), policy in Engram `plan/radicacion-sin-friccion/prod-e2e-policy` and `cashing-backend-master/docs/prod-test-plan.md` layer 1. `playwright.config.ts`: `baseURL` from `PLAYWRIGHT_BASE_URL` (default localhost), `webServer` omitted when remote. **Mechanical write-guard fixture**: `page.route('**/*')` records any non-GET request to a non-localhost host except exactly `POST /api/v1/auth/login`; `afterEach` fails naming method + URL; same guard wraps the `request` fixture; the guard has its own test (a deliberate POST must fail the run). Allowlist: `/health`, login, dashboard, `/contratos` list, contrato detail, cuenta detail, checklist read, `stepper-state`; assertions are shape/render only, skip-when-empty (pattern from `e2e/audit-real-data.spec.ts`). Backend half: `uv run python scripts/smoke_prod.py`. Credentials: `E2E_PROD_EMAIL`/`E2E_PROD_PASSWORD` from git-ignored `.env.e2e.prod` (the dedicated smoke account is created manually by the user, never by a test).
  NOT tested in prod, by decision: radicar, uploads, classification, credits, Google OAuth, Wompi, agent chat.
  Inputs the user must provide before this runs: Vercel production URL; confirm the live API hostname (`https://cashin-api-production.up.railway.app` per `scripts/smoke_prod.py:14`, README mentions another); whether the smoke account exists; prod value of `WAITLIST_ENABLED`.
  Edge cases: remote host with no credentials → authenticated specs skip with a clear message, health still runs; login failure → clear failure, not a cascade; a redirect to a write endpoint is caught by the guard; `PLAYWRIGHT_BASE_URL` set to localhost still boots `webServer`.
  **Done 2026-09-13.** Branch `feat/phase4-prod-smoke`, 7 commits. Round 1 (guard fail-closed axis): CONDITIONAL GO — BLOCKER: the cuenta-page test could never pass against real data because `ChecklistFullView` POSTs `refresh-secop` + `auto-vincular-documentos` on mount (Engram `discovery/checklist-full-view-writes-on-mount`; spec now only visits the write-free `/cuentas-cobro/[id]`); CRITICAL: `guardedFetch` let `options.method` override the classified method (`{method, ...options}` spread order) and the login exception ignored the host (`evil.example.com/api/v1/auth/login` was allowed); WARNINGs: credentials in traces on CI retry, guard at `page.route` missed popups/service workers, silent localhost fallback, `/auth/refresh` POST on 401. Round 2: **NO-GO** — a bare `npx playwright test` collected 0 tests and exited 1 (Playwright picked up the vitest helper test under `e2e/`, and the spec's fail-fast threw at module scope for every project); fixed with per-project `testMatch`, fail-fast in `beforeAll`, constant `.invalid` probe host, `/auth/refresh` allowed with the same origin checks (27 guard unit tests). Verified: `--list` 52/9 (chromium 45/8 as master, smoke 7/1), unit 644, `tsc` clean, mocked specs 7, remote smoke against the real prod API 2 passed / 5 skipped without credentials. PR [#45](https://github.com/Juanpgm/cashing_frontend/pull/45) — **MERGED**.
  Still needed from the user before the authenticated smoke runs: Vercel production URL, live API hostname confirmation, dedicated read-only prod account, prod `WAITLIST_ENABLED`. Follow-up: `test:e2e:staging` script name is still misleading (no-op alias of the default).
  (Full note for slice 1.10's backend half — both branches share the same PR #71 — lives under **1.10**'s own entry above, not repeated here.)

**Phase 4 exit criteria**: `npx playwright test` green in CI with no external network and no client data; every Phase 1–3 slice's named edge cases are green, not just its happy path.

## Phase 5 — Post-checkpoint work merged after `a730acf` / `db45c9f`, and the sdd-verify remediation (added 2026-09-19)

Not in the published plan. These slices were merged to `master` after the as-built artifacts were written; the first `sdd-verify` run (2026-09-18, backend `280fd25` / frontend `7af14b5`) failed on artifact accuracy because they were unrecorded. 5.1–5.4 are the unrecorded work; 5.5–5.8 are the post-verify remediation: WARNING-6 (5.5), WARNING-2 and WARNING-3 (5.6), and a stale e2e fixture plus a load-flaky unit test found by live verification of the merged masters (5.7, 5.8). TDD/mutation evidence is copied from the PR bodies and commit messages; where a PR body is silent it is stated as **not recorded** rather than inferred.

- [x] **5.1** (backend) Semantic per-obligación evidence discovery across Gmail, Drive and Calendar (`POST /integraciones/evidencias/descubrir` returned nothing, or noise, for real contracts — reported case: contract `4161.010.26.1.027.2025`, zero results on all three providers).
  **Done 2026-09-16.** PR [#93](https://github.com/Juanpgm/cashing_backend/pull/93) — **MERGED** (`aa35a4a`, 41 files, +7,598/−318). Root causes fixed: contract número/entidad/objeto were never loaded into the search context (only `fecha_inicio`/`fecha_fin`) and the number's own tokenizer stripped digits; Gmail queries were `subject:`-scoped only; Drive's generic terms never ran once an obligación had 3+ keywords; Calendar ANDed up to 12 terms into one query; shared Drives and Meet links were not covered. As built: every query builder has matchable number variants plus round-robin budgeted slots (the contract number is one signal among several, not the filter); noise filtering, dedup and the LLM relevance prompts carry the contract header and, when available, the user's monthly context; relevance is structured JSON (score + threshold) instead of a bare index list; a semantic query-expansion step (LLM phrases per obligación) is on by default with a deterministic keyword fallback and a credentials/cost guard; ranking blends embeddings, keyword overlap and a capped contract-number bonus, and the LLM verdict is always the final word.
  Edge cases named in the PR: query-budget starvation across all three providers, dedup collapsing distinct items, a tokenizer that scored Spanish stopwords, an LLM-relevance bypass with no safeguard, the LLM-failure fallback (must not emit the whole slate as "alta"), a tolerant JSON parser that must not take the first bracket run of a reasoning preamble. Note: the specs of this change contain no requirement about discovery ranking; this slice is recorded for traceability only.
  Review: 3 rounds of blind adversarial review (two independent judges + a skeptic verification pass per finding, opus). Round 2 fixed 22 confirmed findings from the initial implementation; round 3 fixed 16 more, the two most serious being the first-bracket-run JSON parser (a reasoning preamble could invert the verdict) and an LLM-failure fallback that compared a cosine-inflated score against its keyword threshold (any relevance-LLM error emitted the whole slate as "alta"). TDD / RED-GREEN / mutation evidence: **not recorded** in the PR body. Gate: `GATE OK 4235707 tests=3235 coverage=88% ruff=517 mypy=355 dirty=no`; the previously suspicious `test_give_up_after_failure_is_final_no_zombie_and_no_execution` was isolated and attributed to the pre-existing 120 s `AGENT_TOOL_APPROVAL_TTL_SECONDS`, not a hang.
  Follow-ups tracked (PR body): Drive file content is still not read (filename-only matching); `app/agent/nodes/email_fetch.py` still duplicates the consolidated query-builder logic; multi-account Google support sized but not implemented; Gmail 429 retry/backoff not widened to non-429 transient transport errors (already tracked in 4.3).
- [x] **5.2** (backend) First-cuota-only identity documents in the standard checklist, settled-cuenta data-loss blocker, and step-5 informes gate.
  **Done 2026-09-16.** PR [#94](https://github.com/Juanpgm/cashing_backend/pull/94) — **MERGED** (`46b73d6`, 32 files, +3,401/−128), plus commit `280fd25` (test-only: the 13 new query-expansion tests now monkeypatch `LLM_PROVIDER` too, because a checkout whose git-ignored `secrets/.env.local` sets `LLM_PROVIDER=fake` made the credentials guard skip before reaching the behavior under test — the same class as the 4.7a "discovered, not fixed" note).
  As built (`app/services/checklist_service.py`): CEDULA, RUT, RPC and CDP are requested only on a contract's first cuota (`_PRIMERA_CUOTA_OCULTOS`, unconditional); CONTRATO is first-cuota-only too but reappears on a later cuota when there is no shared contract-level document or the contrato has zero `Obligacion` rows; ACTA_INICIO and FICHA_TECNICA are nivel-contrato and stay always visible. One seam (`requisito_aplica_a_cuenta` / `listar_filas_visibles`) is shared by the checklist view, the resumen, the radicar gate, the constancia PDF and the radicación package. A row that already carries a real document, SECOP link or manual decision (CUMPLIDO_MANUAL / NO_APLICA) stays visible and the package keeps shipping its file (`heredado`). Read-time filtering hides legacy rows materialized before the rule, so no data backfill was needed; migration `043_checklist_primera_cuota_flags.py` updates the catalog flags for existing deployments (the seed only inserts missing codes). `stepper_state_service._step5_formato` now reports `complete` only when both INFORME_ACTIVIDADES and INFORME_SUPERVISION rows are satisfied and exposes `detail.informes_pendientes`; a missing informe row is not pending.
  Edge cases named in the PR and tests: a contrato with no active PRIMERA cuenta (cuota 1 deleted) must not drop identity documents forever — exactly one surviving cuenta (the earliest) is treated as first, and deleting cuota 1 promotes the next open cuenta; a tombstoned first cuota; a `heredado` row must not keep blocking the radicar gate; a custom requisito mapped to CONTRATO must not cancel its own reappearance rule; uploading the reappeared CONTRATO document must not make it vanish from the package that needs it; **BLOCKER (settled cuenta)**: `definir_set` used to delete a settled cuenta's requisitos (the PR names APROBADA/RADICADA; in code the closed set is `_ESTADOS_CUENTA_CERRADA` = `enviada`, `aprobada`, `pagada`) while the new closed-cuenta guard refused to rebuild them, flipping `radicacion_lista` to `True` on an account with zero real soportes — reproduced end to end and fixed by refusing the delete outright on a settled cuenta (CUMPLIDO_MANUAL/NO_APLICA get an explicit content exception; an admin escape-hatch function exists for future catalog changes). Commit message records that the shared test cuenta fixtures never set `posicion` (silently RECURRENTE) and were fixed to derive PRIMERA/RECURRENTE like `crear_cuenta_cobro`.
  Review: 4 rounds of blind adversarial review (two judges + skeptic pass, opus). R1 NO GO (contract-reappearance rule self-defeating; contrato-with-no-first-cuota gap); R2 CONDITIONAL GO (the R1 fix for custom/inferred mappings never reached the READ path, plus data-visibility edge cases); R3, narrowed to the two riskiest behavioral changes, found the settled-cuenta BLOCKER above. The round-4 outcome is **not recorded** in the PR body. TDD / mutation evidence: **not recorded** in the PR body. Gate: `GATE OK 8cd6f68 tests=3039 coverage=88% ruff=512 mypy=338 dirty=no`.
  Follow-ups tracked (PR body): `document_service.py`'s contract-replacement branch can leave a stale `CARGADO` row with no artifact on Postgres in a scenario SQLite's tests cannot reproduce (pre-existing, now leaned on more); no frontend e2e exercises the "archivo"/"texto" checklist inference modes end to end. The frontend seed helper that assumed all eight document types on every cuota broke against this rule and was fixed in 5.7.
- [x] **5.3** (frontend) Staged Pantalla 2 — Contexto first, one active section at a time, one completeness model. Supersedes 3.4's `revealMode: "all"`.
  **Done 2026-09-16.** PR [#68](https://github.com/Juanpgm/cashing_frontend/pull/68) — **MERGED** (`a93b16e`, 80 files, +9,241/−779). Pantalla 2 ("Completá tu cuota") went from one screen with four sections mounted at once to a staged rail: **Contexto → Evidencias (raw step 4) → Justificaciones (6) → Soportes (3) → Formatos e informes (5)**. As built: `GROUPS[1]` is `members: [4, 6, 3, 5]`, `stagedOrder: [CONTEXTO_MEMBER, 4, 6, 3, 5]`, `revealMode: "staged"`; `members` stays raw-steps-only so `furthestCompletedGroup` (`Math.max`), `canNavigateToGroup` (`Math.min`) and the footer's blocking reason never depend on display order or on the virtual Contexto section. New "¿Qué hiciste este mes?" Contexto section opens the screen (`contexto-section.tsx`; the choice "Continuar sin contexto" is per-browser, keyed by cuenta id, in `contexto-omitido.ts`); `section-model.ts` is the single pure model (`computeSectionModel`, called once per render by `StepperShell`) that the rail, collapsed rows, done-acknowledgement, closing receipt, footer label/hint and group gate all read — it separates **`done`** (the backend calls the section finished; the only thing allowed to claim completion) from **`passable`** (the walk may move on: `done`, or Evidencias with at least one persisted evidencia); `staged-section-list.tsx` (sticky five-stage rail header, collapsed summaries with "Editar", locked stubs with one plain-language next action, done-ack with "Continuar"); `hooks/use-done-ack.ts`; the checklist inside Pantalla 2 shows only upload-type "Soportes" (`ChecklistFullView variante="soportes"`, `categoriaRequisito`, `resumenDeItems` using the backend's own counting rules) while the two auto-generatable informes moved into "Formatos e informes" and evidence coverage lives only in its own step; `use-requisito-patch.ts` extracted; terminology pass ("obligación"/"actividad contractual" for what the contract requires, "justificación" for what the AI writes). Two env-gated screenshot passes arrived at the `e2e/` root (`screens-r4.spec.ts` needs `R4_SCREENS=1`, `screens-r5.spec.ts` needs `R5_SCREENS=1`) plus `e2e/helpers/staged-rail.ts`.
  Edge cases covered by tests (verified by reading the test names, not inferred): incomplete Contexto keeps Evidencias and everything after it locked and does not auto-fire evidence discovery, yet never blocks the footer/radicación (`groupDone` ignores Contexto); the Contexto section stays active while the cuenta query is in flight and counts as done when the cuenta is no longer editable or its query failed (no dead end); zero-obligaciones contrato advances the walk on the backend's own vacuous completeness; Evidencias "secured" (1 of N) is `partial`, pill "Parcial", never "Completo"; a section the backend calls done never renders locked, with or without a pending ack; the receipt "Todo listo" needs the rail at the end, every rail section done and every raw member done, "Casi listo" otherwise, none while an ack is pending or the rail is at an earlier section; Formatos e informes follows `StepState.complete` and `detail.informes_pendientes` (informe codes are humanised, never shown raw); resuming mid-stage derives the active section from backend state, shows no ack for a section already complete on mount and does not greet the user with work that finished while they were on another group; the last section has no ack; the debounce flushes on unmount and keeps a failed-flush draft; `prefers-reduced-motion` scrolls instantly; focus is not moved by a data transition, only by the user's own "Continuar"; the newly active section is announced in a polite live region; rail marks keep the full section name as visually hidden text while shortening the visible label at phone width. `use-group-auto-advance` never auto-advances out of a staged group.
  Review: **7 rounds** of blind adversarial review (two independent judges + a skeptic verification pass per finding, opus). Rounds 1–2 fixed the initial staged reveal (zero-obligaciones lock, cross-group auto-advance, a lying "Completo" pill, weakened e2e assertions, tier-vs-backend count mismatches, an unreachable "no evidencias" escape hatch) and added Contexto, the reorder and the terminology pass. Round 3 replaced the scattered completeness helpers with `section-model.ts` after the rail, receipt, footer and group gate were found disagreeing. Rounds 4–7 each found a real regression inside the previous round's own fix, always the same shape — state written but never compared by the effect/predicate that reacts to it; round 7 closed the class by funnelling `contexto-mensual.tsx`'s eight mutable references through one predicate. TDD: the PR body states every fix was TDD'd (failing regression test first) and re-verified against the live app; per-test RED/mutation detail is **not recorded**. Gate: `GATE OK 9b29498 unit=1330 e2e=7 tsc=clean lint=backlog(5e/8w) build=ok dirty=no node=25`. Live stack (`LLM_PROVIDER=fake`, no mocking): `e2e/journey` green in 9 of 9 consecutive runs across the review rounds (2 isolated failures traced to the pre-existing external flake in `secop-flujo.spec.ts` hitting live datos.gov.co); `e2e/edge-cases` and the mocked specs green throughout.
  **OPEN USER DECISION (recorded, not decided): group-2 height.** The 3.4 question — collapse members by default / accordion / keep — is not answered by any artifact. What changed: PR #68's body says the staging mitigates it because only one section renders at a time; that is context for the decision, not the decision. It stays open in `design.md` Open Questions and `specs/stepper-ux/spec.md`.
  Follow-ups tracked (PR body): `e2e/smoke` intentionally not run locally (needs `PLAYWRIGHT_BASE_URL`); `sinGuardar` (Contexto dirty flag) stays `true` after a cuenta becomes non-editable mid-session (deliberate, self-heals on the next keystroke); Pantalla 3 still uses some older copy ("Ir al paso 2", "validando checklist"). The two `e2e/screens-r*.spec.ts` files sit outside the `journey|edge-cases|smoke` layout that `specs/quality-gate` mandates (env-gated screenshot passes; not sanctioned by 4.7a).
- [x] **5.4** (frontend) Direct wizard entry point on the account detail page.
  **Done 2026-09-16.** Commit `7af14b5` on frontend `master` (no PR number; 2 files, +38): the wizard route `/radicar/[cuentaId]` already supported being scoped to an existing cuenta but nothing on `/cuentas-cobro/[id]` linked to it — the only reachable entry was the sidebar's generic `/radicar` flow, which starts by asking for a contrato. `app/cuentas-cobro/[id]/page.tsx` now renders a primary "Completar en el wizard" link (`data-testid="cuenta-completar-wizard-btn"`, `href=/radicar/{id}`) only while the cuenta is `borrador` or `rechazada`, and demotes "Generar con IA" to `variant="neutral"`. Edge cases (test names in `app/cuentas-cobro/[id]/__tests__/page.test.tsx`): visible with the right `href` for `borrador` and `rechazada`; hidden for `enviada`, `aprobada` and `pagada`. Review: **not recorded** (direct commit, no PR body). TDD/mutation: **not recorded** (tests added in the same commit).
- [x] **5.5** (backend) Stale-job reset made race-safe in both job upserts — closes the first `sdd-verify` run's WARNING-6.
  **Done 2026-09-19.** PR [#95](https://github.com/Juanpgm/cashing_backend/pull/95) — **MERGED** (`81aab19`, 4 files, +689/−2). As built: `evidence_classification_service._upsert_job` now forces `job.updated_at = datetime.now(UTC)` on the stale-reset branch (a stale PENDING row with the same `total`, `procesadas == 0` and `error is None` was a no-op reassignment, SQLAlchemy emitted no UPDATE, so `onupdate=func.now()` never fired and the staleness clock never reset); **both** `_upsert_job` functions (classification and paquete) re-select under `FOR UPDATE` with `populate_existing=True` (without it the locking select returns the already-mapped stale instance, so a second caller keeps the old `status`/`updated_at` and double-enqueues); `paquete_job_service._upsert_job`'s reset nulls the previous run's result payload (`storage_key`, `filename`, `size_bytes`, `listo_para_radicar`, `pendientes`, `advertencias_coherencia`, `es_borrador`) so a `pending` job no longer exposes the previous package to a poller (API response shape unchanged).
  Edge cases: stale PENDING vs stale RUNNING (only PENDING with equal `total` was value-identical); a genuine two-session interleaving test; a second symptom the interleaving test exposed (winner finishes the job, loser's assignments equal its stale cache → no UPDATE, DB row stays `done` while a run is scheduled); the paquete stale-payload gap. Covering tests present on master: `tests/test_evidence_classification_concurrency.py` (`test_stale_pending_job_noop_reset_is_not_re_stale_for_a_second_caller`, `test_stale_pending_job_reset_moves_updated_at_forward`, `test_loser_with_stale_identity_map_sees_winners_fresh_reset_and_does_not_enqueue`, `…_after_winner_finished_job_resets_from_fresh_state`, `test_loser_is_still_reset_when_row_is_genuinely_stale_and_winner_did_nothing`, `test_stale_threshold_boundary_for_pending_and_running`) and `tests/test_paquete_job_service.py` (`test_reset_clears_previous_run_result_payload`, `test_polling_a_done_job_preserves_its_result_payload`, plus the same three `test_loser_*` identity-map tests).
  Review and TDD: strict TDD in every round, RED first, mutation-proved (PR body). r1 (opus) **CONDITIONAL GO** — found that the lock re-select did not refresh the identity map, so the `updated_at` bump never reached the second caller; fixed with `populate_existing` plus the two-session interleaving test. r2 (opus) **GO** — verified by mutation that the new tests discriminate, and found the paquete stale-payload gap, fixed in round 3 together with two SUGGESTIONs. Gate: `GATE OK 8f9bf9f tests=3330 coverage=88% ruff=526 mypy=357 dirty=no`. The real-Postgres suite was skipped (gate default; Docker daemon not running locally): the SQLite tests prove the identity-map defect and its fix, not real lock serialization.
  Follow-ups tracked (PR body, non-blocking): run the two touched test files against real Postgres (`scripts/pre-merge.ps1 -IncludePg`); no heartbeat during the LLM batch in `_ejecutar_clasificacion` (a job can exceed the 120 s stale window while alive; pre-existing); `secop_service.py:1443-1448` has the same identity-map trap on a second locking select (verified 2026-09-19: that select still carries no `populate_existing`); the reset writes `updated_at` from the app clock while other writes use `func.now()` (cosmetic at a 120 s threshold); pre-existing mypy error in `tests/test_paquete_job_service.py` (`__table__.name`).
- [x] **5.6** (frontend) Staged progressbar accessible name and `.detail` reads in `lib/**` — closes the first `sdd-verify` run's WARNING-2 and WARNING-3.
  **Done 2026-09-19.** PR [#69](https://github.com/Juanpgm/cashing_frontend/pull/69) — **MERGED** (`7fddacb`, 5 files, +246/−11). `components/ui/progreso-etapas.tsx`: the `role="progressbar"` now has `aria-label={label || "Progreso"}` (reuses the existing `label` prop; step 6 passes "Generando justificaciones…", step 7 "Generando el paquete…"). `lib/checklist-api.ts` `extractUploadErrorMessage` no longer reads `.detail` by hand — it uses `hasApiErrorDetail`/`extractApiError`; ESLint on `lib/**` went from 2 errors to 0 and the PR reports no other direct `.detail` reads in `lib/**`. Two intentional user-visible message changes on upload errors: a 422 array `detail` no longer shows the raw `body.files:` loc prefix, and an object/junk `detail` now yields the generic Spanish fallback instead of raw JSON (matches the "no raw backend text" rule). Edge cases: 422 array detail, object detail, junk detail, 429 branch unchanged, `CHECKLIST_LINK_FAILED` path byte-identical.
  Review and TDD: strict TDD — RED first for 15 tests, confirmed by mutation (both fixes reverted → 15 fail). Fresh-context adversarial review (opus): **GO**; no production caller depended on the old message text; 7 SUGGESTIONs, S1/S2/S3/S6 applied in a second commit (comments, one duplicate test removed). Gate: `GATE OK d9708d9 unit=1355 e2e=7 tsc=clean lint=backlog(3e/8w) build=ok dirty=no node=25`.
  Follow-ups tracked (PR body): `detail: ""` still produces a message-less error (pre-existing, `lib/api.ts:189`); a string-array `detail` is no longer joined (no backend producer); the ESLint `.detail` selector is repo-wide and untyped — a narrower `data.detail` selector would avoid false positives.
- [x] **5.7** (frontend) E2E seed helper follows the requisitos actually materialized on the cuota; double-click guard test hardened.
  **Done 2026-09-19.** PR [#70](https://github.com/Juanpgm/cashing_frontend/pull/70) — **MERGED** (`d369fbe`, 5 files, +384/−40). Live verification of the merged masters found one real failure: journey `diez-ejercicios-radicacion` Ejercicio 8 (second cuota) failed 3/3 with a 404 in `seedChecklist`, because 5.2 intentionally stops materializing CEDULA/RUT/RPC/CDP (and CONTRATO when a shared contract document exists) on later cuotas while the helper still uploaded all eight tipos — a stale test fixture, not a product bug. `e2e/helpers/seed.ts`: after `POST /requisitos` the helper reads `GET /cuentas-cobro/{id}/checklist` (the route the UI uses) and uploads only requisitos that exist; signature unchanged; a first cuota still uploads all eight. Loud failures: checklist fetch 4xx/5xx, non-JSON, `requisitos_definidos=false`, empty checklist, malformed item (names the index), none of the seed tipos present, and a **floor** of the four always-applicable codes (SEGURIDAD_SOCIAL, INFORME_ACTIVIDADES, INFORME_SUPERVISION, ACTA_INICIO) so the helper cannot silently under-seed a first cuota and hide a backend regression that used to 404 loudly; custom requisitos (`requisito_cuenta_id` set) are not counted by code. `step-2-cuota.test.tsx` / `vitest.setup.ts`: the double-click guard test hit one `waitFor` 1000 ms timeout under CPU load — diagnosed as test timing, not a product race (128 clean runs; removing the `submitLockRef` guard fails deterministically with "called 2 times"); `asyncUtilTimeout` set to 5000 and microtasks flushed before asserting the call count, so the test proves the second click was swallowed, not merely late.
  Review and TDD: fresh-context review (opus) of the first commit **CONDITIONAL GO** — tipo-to-code mapping verified against the backend catalog; no race between `POST /requisitos` and `GET /checklist`; WARNING (silent under-seed, no floor) and WARNING (test hand-copied the tipo list) fixed, plus suggestions on malformed items, custom codes and stale comments. TDD: 15 tests RED before the floor. **The second-round commits (`1bc3e8a`, `4b21643`) are test-only and did NOT get a second fresh review**; they are covered by RED evidence, the mutation check on the double-click guard, the live runs and the gate. Gate: `GATE OK 4b21643 unit=1389 e2e=7 tsc=clean lint=backlog(3e/8w) build=ok dirty=no node=25`. Live (backend master `81aab19`, `LLM_PROVIDER=fake`, SQLite): journey 13/13; `radicacion-paquete-completo` plus edge-cases 26 passed / 1 skipped (by design); five other journey specs that use the helper 23/23 (earlier run on the first commit).
  Follow-ups tracked (PR body): the floor assumes `modo: "estandar"` on a BORRADOR cuenta; `ChecklistBody` in the helper duplicates a slice of `lib/checklist-api.ts` (e2e/ imports nothing from lib/), so a backend field rename would only surface in the live journey; `scripts/kill-local.ps1` is broken on PowerShell 7 (assigns to the read-only `$pid`).
- [x] **5.8** (frontend) Load-flaky unit tests kept inside vitest's wall-clock timeout.
  **Done 2026-09-19.** PR [#71](https://github.com/Juanpgm/cashing_frontend/pull/71) — **MERGED** (`11bf4c4`, 2 files, +2/−3). vitest's per-test timeout is wall-clock, so an OS-starved process fails tests that need ~10–17 ms of work. `components/stepper/__tests__/contraste.test.ts`: the first `describe` gets `{ timeout: 30_000 }` (its body is synchronous, walks 87 files 5×). `lib/__tests__/api.test.ts`: a static top-level `import instance from "@/lib/api"` replaces `await import()` inside the only test after `vi.resetModules()` (which paid a cold vite-node RPC inside its own 5000 ms budget); the now no-op `vi.resetModules()` is dropped.
  Evidence (64 busy-loop burners, 32 logical cores, identical load before/after): protocol A (the two files together, 40 iterations) contraste **18/40 timeouts → 0/40**; protocol B (full suite, 8 iterations) contraste 4/8 → 0/8; `api.test.ts` 0/48 before and after. The `api.test.ts` change has **no measured defect to improve on** (it never reproduced in 48 stressed runs); it is justified by mechanism only (worst test time 1450 ms → 4 ms of a 5000 ms budget) and the PR calls it the droppable half. Injected defects still fail on their normal assertion, not a timeout: a forbidden muted class in a watched file fails with the 4.5:1 contrast message naming file:line, and removing `timeout: 30_000` from `lib/api.ts` fails `expect.objectContaining({ timeout })` (also confirms the static import does not break `vi.spyOn(axios, "post")`).
  Review: test-only change, **no fresh-context review** (PR body); covered by the before/after stress counts and the injected-defect checks. RED/GREEN in the TDD sense: not applicable (timing fix; the before/after stress protocol is the evidence). Gate: `GATE OK b19179f unit=1389 e2e=7 tsc=clean lint=backlog(3e/8w) build=ok dirty=no node=25`.
  Follow-ups tracked (PR body): `components/__tests__/checklist-full-view.test.tsx` "chunks a drop of more than 20 files into sequential batches of 20" timed out 3/8 in the stressed full-suite runs (worst passed 4935 ms) — not fixed, the next likely spurious red gate (the 3/8 → 0/8 seen after this change is noise at n=8); other tests near the budget under load: stepper-radicar (3690 ms), step-1-contrato (2625 ms), contratos/[id] page (2505 ms); optional hygiene: memoize the contraste walk (5 walks → 1).

**Phase 5 exit-criteria impact**: no Phase 0–4 exit criterion changes state. The Phase 3 criterion (3-user unmoderated test) still has no recorded evidence and remains unverified, not failed. The Phase 4 clause "every slice's named edge cases green" is now met for the previously open accessible-name gap (5.6). The Phase 2 clause "checklist load under 1s against Neon dev" still has no recorded measurement.

---

## Review Workload Forecast

- Backend touches ~14 files across Phase 1–2 non-trivially (agent tools, cuenta_cobro_service, checklist_service, informe_service, models). Frontend touches ~20 files across Phase 3, with 2 large mechanical splits (3.1) counted separately from behavior changes.
- 400-line budget risk: **High** for 2.4 (selectin removal touches every call site that assumed the cascade) and 3.1 (pure splits, large diff but zero logic risk) — both flagged for possible sub-splitting at apply time.
- Chained PRs recommended: **Yes**, `stacked-to-main` per decision #3 above. One PR per numbered slice; 2.4 and 3.1 may split further (e.g. 2.4a models+raise_on_sql, 2.4b explicit selectinload call sites) if the diff exceeds ~400 lines once real files are touched.
- Decision needed before apply: only if a slice's actual diff exceeds ~400 lines and isn't cleanly sub-splittable — escalate per `ask-on-risk` (decision #3), otherwise proceed.
