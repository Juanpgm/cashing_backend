## Summary

<!-- What does this PR do and why? -->

## Verification (local gate — GitHub Actions is billing-locked)

<!--
Paste the literal `GATE OK ...` line printed by scripts/pre-merge.ps1
(run via the pre-push hook, or manually: .\scripts\pre-merge.ps1).

Requirements:
- <sha> must equal this PR's head commit.
- dirty=no (no uncommitted changes when the gate ran).
-->

```
GATE OK <sha> tests=<n> coverage=<pct>% ruff=<clean|N> mypy=<clean|N> dirty=no
```

## Review history

<!-- Notable review rounds, re-reviews, or resolved disagreements. -->

## Follow-ups

<!-- Deferred work, known debt, or things to revisit later. -->
