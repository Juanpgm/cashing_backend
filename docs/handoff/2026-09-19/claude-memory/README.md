# Exportación de la memoria de Claude (2026-09-19)

Estos archivos son una copia de `~/.claude/projects/C--Users-User-Documents-workspace-cashing/memory/` de la máquina original. La memoria de Claude Code **no se sincroniza entre máquinas**; esto es solo un respaldo legible.

Para restaurarla en la laptop:

1. Abre Claude Code una vez en la carpeta del workspace para que cree su carpeta de proyecto en `~/.claude/projects/<slug-de-la-ruta>/`.
2. Copia estos `.md` a `~/.claude/projects/<slug>/memory/` (el `<slug>` cambia según la ruta del workspace en la laptop; no reutilices el slug de esta máquina).
3. `MEMORY.md` es el índice: si ya existe uno en la laptop, **fusiona** las líneas en vez de sobrescribirlo. Las rutas absolutas y de Windows que aparecen dentro de las notas son de la máquina original.

Contenido más importante: `project_verify_cierre_20260919.md` (estado y pendientes), `plan_radicacion_sin_friccion.md` (historial del plan), `feedback_tdd_proporcional.md` y `feedback_local_first.md` (reglas del usuario) y los `gotcha_*` (trampas técnicas).

No contienen credenciales (se revisó con patrones de secretos antes de subirlos).
