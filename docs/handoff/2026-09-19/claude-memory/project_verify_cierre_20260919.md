---
name: project-verify-cierre-20260919
description: Cierre 18-19/09 del plan radicar sin fricción — sdd-verify FAIL solo por artefactos; warnings W2/W3/W6 y e2e/flaky arreglados y mergeados; pendientes reales
metadata: 
  node_type: memory
  type: project
  originSessionId: f5ec3edf-f437-4244-8541-ed31d1cb8849
  modified: 2026-09-20T00:15:47.963Z
---

**Estado 2026-09-19.** Masters: backend `81aab19` (PR #95), frontend `11bf4c4` (PRs #69 #70 #71). Plan [[plan-radicacion-sin-friccion]] completo; spec + design.md se escribieron RETROACTIVOS (openspec/changes/radicacion-sin-friccion/) para desbloquear el dispatcher nativo (`gentle-ai sdd-status`). `verify-report.md` ahí: FAIL solo por exactitud de artefactos (2 CRITICAL: `tasks.md` no registra el PR #68 frontend `a93b16e` "staged Pantalla 2" ni `7af14b5`; `specs/stepper-ux` dice revealMode "all" pero master usa "staged"). Gates y suites verdes (backend 3329, frontend 1389).

**Arreglado y mergeado:** W3 `.detail` a mano en `lib/checklist-api.ts` (#69), W2 aria-label en `ProgresoEtapas` (#69), W6 `_upsert_job` (#95). Journey Ejercicio 8 fallaba por seed desactualizado tras backend PR #94 (solo-primera-cuota: CEDULA/RUT/RPC/CDP no se materializan en cuotas posteriores) → `seedChecklist` ahora lee el checklist real + piso de 4 códigos siempre-aplicables (#70). Flaky `contraste.test.ts` (18/40 → 0/40 bajo 64 burners) y `api.test.ts` (#71).

**Why:** el usuario pidió arreglar los warnings primero y que todo funcione bien antes de archivar.

**How to apply / gotchas:**
- `select().with_for_update()` sobre una fila ya en el identity map NO refresca columnas → hace falta `.execution_options(populate_existing=True)` (precedente `cuenta_cobro_service.py:235-259`). Mismo defecto sin arreglar en `secop_service.py:1443-1448`. Un test de carrera debe cargar la fila en la sesión B ANTES de que A commitee.
- El timeout de vitest es de RELOJ DE PARED (`Date.now`): un proceso sin CPU falla tests de 10 ms. Un cuerpo síncrono nunca es interrumpido por el timer.
- El JSON reporter de vitest redacta los fallos como `STACK_TRACE_ERROR`; en PowerShell `$args` es variable automática (no usarla como nombre local).
- `scripts/kill-local.ps1` (backend) está roto en PowerShell 7 (asigna a `$pid`, read-only) → matar por PID. `cashing-frontend/.env.local` apunta al puerto viejo :8003 (no commitear; setear `NEXT_PUBLIC_API_URL` por proceso).
- Docker Desktop apagado → suite contra Postgres real NUNCA corrió para #95 (`scripts/pre-merge.ps1 -IncludePg`); `FOR UPDATE` es no-op en SQLite.

**Actualización 19/09 (tarde):** SDD re-verify = PASS WITH WARNINGS (0 CRITICAL) y cambio ARCHIVADO en `openspec/changes/archive/2026-09-19-radicacion-sin-friccion/` (el agente archivador reportó éxito con el trabajo a medias: verificarlo a mano; queda una carpeta duplicada en `openspec/changes/radicacion-sin-friccion/` que el usuario debe borrar). PR #95 verificado en PostgreSQL 16.15 REAL (rama dev de Neon, endpoint `ep-autumn-union-acgvv03i`): 13/13 concurrencia = 1 encolado (el código previo encola 2). Producción: solo capa 1 anónima (`smoke_prod.py`); el build de prod es ANTERIOR al PR #94/#95 (diff de OpenAPI: falta el campo `heredado`); su cadena LLM tiene un 3er modelo retirado. Bug real del frontend arreglado en PR #72 (master `f7e69c4`): el resume de `use-group-auto-advance.ts` pisaba un clic hecho antes de que llegara `stepper-state`. Informes: `context/reporte-cierre-2026-09-19.md`, `context/neon-full-flow-2026-09-19.md`, `context/adr-codebase-memory-2026-09-19.md`. GOTCHA: un `index_repository` full BORRA el ADR de codebase-memory-mcp (restaurar desde el archivo de respaldo). Residuos: 49 usuarios `e2e-*@example.com` en Neon dev (borrado en cascada pendiente, pedir OK). Sigue abierto: decisión de desplegar (correr antes `scripts/pre-merge.ps1 -IncludePg` con Docker), capa 1 autenticada (faltan credenciales), decisión UX grupo 2.

**DESPLEGADO 19/09 (noche):** backend `81aab19` a Railway prod (deployment `c533ab53`, SUCCESS) con autorización explícita. El deploy del backend es MANUAL (`railway up`; `CLAUDE.md:153-155` "CI/CD automático" es FALSO); el frontend (Vercel) sí auto-despliega desde master (ya estaba en `f7e69c4`). Prod estaba en escenario A (`alembic_version=041`, `paquete_job` creada por `create_all`): se hizo stamp 041→042 en prod (write autorizado) y al arrancar corrió la 043: flags RPC/CDP/CONTRATO=True, `heredado` en OpenAPI, `/health/llm` degraded→ok tras cambiar `LLM_PRODUCTION_FALLBACK_MODEL` a `gemini/gemini-3.6-flash`. Neon dev limpiado (49 usuarios e2e, 1478 filas). Rollback degradado: el deployment previo quedó REMOVED → reconstruir `a730acf` en un worktree + `railway up`. ⚠️ CREDENCIAL EXPUESTA: un subagente volcó `FIREBASE_SERVICE_ACCOUNT_JSON` con `railway variables --kv` → el usuario debe ROTAR la clave de la service account (proyecto `cashing-9b4f6`); NUNCA usar `--kv`. Pendiente: rotar clave, arreglar raíz create_all-vs-alembic (migraciones idempotentes), corregir `CLAUDE.md`, capa 1 autenticada de prod (sin credenciales), suite PG del repo (Docker), vigilar costo/latencia de `EVIDENCE_QUERY_EXPANSION_ENABLED` (#93 lo activa por defecto), decisión UX grupo 2, borrar carpeta duplicada de openspec.

**Cierre posterior (19/09 noche):** backend PR #96 mergeado (master `b65f872`): migración 042 idempotente + guardián de migraciones (`tests/test_migration_convention_guard.py`, exime solo prefijos numéricos ≤043; `make migration` genera nombres hash) + `scripts/check_alembic_state.py` de solo lectura + `CLAUDE.md` corregido + `docs/deploy-runbook.md`. NO desplegado: prod sigue en `81aab19` con alembic en 043; recomendé desplegarlo junto con la siguiente tanda. Grafo de código desactualizado para #96 (reindexar borra el ADR: restaurar desde `context/adr-codebase-memory-2026-09-19.md`). Pendientes reales: rotar clave Firebase, capa 1 autenticada de prod (credenciales), suite PG con Docker (incl. `TestReadOnlyOnPostgres`), decisión UX grupo 2, borrar carpeta duplicada de openspec, vigilar `EVIDENCE_QUERY_EXPANSION_ENABLED`.

**Pendiente (versión original):** (1) corregir `tasks.md` + `specs/stepper-ux` (solo docs) → re-correr sdd-verify → sdd-archive; (2) decisión UX del usuario sobre altura del grupo 2; (3) `checklist-full-view.test.tsx` "chunks a drop of more than 20 files…" falla 3/8 bajo carga (próximo gate rojo espurio); (4) Postgres real con Docker; (5) follow-ups: sin heartbeat durante el batch LLM (stale ≠ dead), `detail:""` sin mensaje, selector ESLint `.detail` repo-wide, fix `$pid` en kill-local.
